---
title: "URLs, DNS, and HTTP"
description: "What happens between hitting Enter on a URL and seeing the page."
phase: 0
module: "0-3-how-the-internet-works"
order: 2
estimatedMinutes: 9
difficulty: 1
standards:
  cs2023: ["NC-2"]
  swebok: ["Computing Fundamentals"]
  bloom: "understand"
  sfia: 2
  dreyfus: "novice"
vocabulary: ["http", "dns"]
prerequisites: ["0/0-3/09"]
learningOutcomes:
  - "Identify the host and path in a URL"
  - "Explain DNS name lookup separately from HTTP requests"
  - "Interpret 200, 404, and 500 as different HTTP outcomes"
thresholdConcept: false
---

## Why this matters

You type `dura.dev` into your browser and a page appears. Half a dozen distinct technologies cooperate to make that happen, and every one of them can fail. Knowing what each piece does turns "the internet is broken" into "DNS is slow today."

## Start with something familiar

Imagine looking up a shop’s address before sending an order for one item. Finding the address and asking for the item are separate tasks.

## The mental model

> DNS finds an address for a name; HTTP carries a request and a response describing the requested resource.

## Anatomy of a URL

A URL has more parts than people realize.

```text
https://dura.dev/dictionary/algorithm?from=lesson#definition
└─┬─┘   └───┬──┘└────────┬───────────┘└────┬────┘└────┬────┘
scheme   host           path             query     fragment
```

- **Scheme** — `https` says "use the secure HTTP protocol."
- **Host** — `dura.dev` is the human name of the server.
- **Path** — `/dictionary/algorithm` selects a specific resource on that server.
- **Query** — `?from=lesson` carries extra parameters.
- **Fragment** — `#definition` jumps to a section after the page loads (the server never sees it).

## DNS: name → number

Computers don't talk to each other by name. They talk by IP address — `142.250.65.78` style. DNS (Domain Name System) is a giant distributed phone book that translates names into addresses.

When you type `dura.dev`, your computer asks a DNS server "what's the IP for dura.dev?" The DNS server replies with a number, and the browser uses that number to actually connect.

DNS results are cached (saved temporarily for reuse), which is why you sometimes see "DNS still propagating" after a domain change — the old cached answer hasn't expired everywhere.

## HTTP: how the conversation happens

In this HTTP/1.1 example, after address lookup and connection setup the browser sends an HTTP **request**. HTTPS also protects the exchange with encryption. These are message fragments for reading, not terminal commands:

```text
GET /dictionary/algorithm HTTP/1.1
Host: dura.dev
Accept: text/html
```

The server replies with an HTTP **response**:

```text
HTTP/1.1 200 OK
Content-Type: text/html

<!DOCTYPE html>...
```

This is the request/response pattern. Status codes tell you what happened: `200` means success, `404` means not found, and `500` means a server-side error. Headers carry descriptive information, such as the content type. The body carries the actual content.

## Guided practice

In https://example.com/notes, which part is the host and which part selects the resource?

Predict on paper or in your head before opening the answer; no installation or internet connection is needed.

<details>
  <summary>Reveal the practice answer</summary>

example.com is the host. /notes is the path selecting the resource.

</details>

<FillBlank
  prompt="The system that turns 'dura.dev' into an IP address is ___. The protocol your browser uses to fetch pages is ___."
  answers={["DNS", "HTTP"]}
  hints={["Three letters.", "Four letters."]}
/>

## Check your understanding

<Quiz
  questions={[
    {
      type: "multiple-choice",
      question: "What does DNS do?",
      options: [
        "Encrypts your traffic",
        "Translates domain names into IP addresses",
        "Compresses web pages",
        "Blocks ads",
      ],
      correct: 1,
      explanation: "DNS is the system that maps human-friendly names to numeric IP addresses.",
    },
    {
      type: "multiple-choice",
      question: "What does an HTTP status of 404 mean?",
      options: ["Server error", "Not found", "Forbidden", "Successful"],
      correct: 1,
      explanation:
        "The 4xx codes are client errors. 404 specifically means the resource doesn't exist.",
    },
  ]}
/>



## What you learned

- Identify the host and path in a URL.
- Explain DNS name lookup separately from HTTP requests.
- Interpret 200, 404, and 500 as different HTTP outcomes.

Continue with [the next lesson](/paths/0/0-3/11).
