---
title: "Navigating Files"
description: "pwd, ls, cd. The three commands you'll use a thousand times."
phase: 0
module: "0-2-your-first-terminal"
order: 2
estimatedMinutes: 8
difficulty: 1
standards:
  cs2023: ["SP-1"]
  swebok: ["Software Engineering Tools and Methods"]
  bloom: "understand"
  sfia: 2
  dreyfus: "novice"
vocabulary: ["terminal"]
prerequisites: ["0/0-2/05"]
learningOutcomes:
  - "Identify pwd, ls, and cd by their effects"
  - "Trace a move into a child directory and back to its parent"
  - "Distinguish a current directory from a parent directory"
thresholdConcept: false
---

## Why this matters

Before you can _do_ things in a terminal, you need to know where you are. Three commands handle nearly all navigation. They are short, ugly, and you will type them so often they become muscle memory within a week.

## Start with something familiar

Imagine walking through rooms in a building. You need a location, a view of the doors, and a way to enter another room.

## The mental model

> The shell has one current directory; relative paths are interpreted from that location.

These are example transcripts for a Unix-style shell such as bash or zsh. Lines beginning with $ show commands; other lines show sample output. Folder names must exist to navigate into them. PowerShell has different options for some commands.

## pwd: where am I?

`pwd` stands for **print working directory**. It tells you exactly where in the filesystem you currently are.

```text
$ pwd
/Users/dustin/projects/dura
```

A path like `/Users/dustin/projects/dura` is a list of nested folders separated by slashes, starting from the root `/`. Every file and folder on your computer has one of these.

## ls: what's here?

`ls` lists the contents of the current directory.

```text
$ ls
README.md  package.json  src
```

Add `-la` for the long view with hidden files:

```text
$ ls -la
drwxr-xr-x   8 dustin staff   256 Apr  8 10:24 .
drwxr-xr-x   5 dustin staff   160 Apr  8 09:11 ..
-rw-r--r--   1 dustin staff   532 Apr  8 09:11 .gitignore
-rw-r--r--   1 dustin staff  1820 Apr  8 09:30 README.md
```

A name beginning with a dot, such as `.gitignore`, is normally hidden from a plain `ls` listing. `.` means "this directory" and `..` means "the parent directory."

## cd: go somewhere

`cd` changes directory. You give it a path.

```text
$ cd src
$ pwd
/Users/dustin/projects/dura/src
$ cd ..
$ pwd
/Users/dustin/projects/dura
```

`cd` with no arguments takes you home. `cd -` takes you back to where you were. Tab completion fills in long names — start typing and hit `Tab`.

## Guided practice

Starting at /home/learner, what location follows cd notes and then cd .., assuming notes exists?

Predict on paper or in your head before opening the answer; no installation or internet connection is needed.

<details>
  <summary>Reveal the practice answer</summary>

The first command moves to /home/learner/notes. The second returns to /home/learner.

</details>

<FillBlank
  prompt="To list files use ___, to change directories use ___, to print where you are use ___."
  answers={["ls", "cd", "pwd"]}
  hints={["Two letters.", "Two letters.", "Three letters."]}
/>

## Check your understanding

<Quiz
  questions={[
    {
      type: "multiple-choice",
      question: "Which command tells you your current directory?",
      options: ["ls", "cd", "pwd", "where"],
      correct: 2,
      explanation: "pwd = print working directory.",
    },
    {
      type: "multiple-choice",
      question: "What does `..` refer to?",
      options: [
        "The current directory",
        "The parent directory",
        "The home directory",
        "The root directory",
      ],
      correct: 1,
      explanation: "Two dots is shorthand for the directory one level up.",
    },
  ]}
/>



## What you learned

- Identify pwd, ls, and cd by their effects.
- Trace a move into a child directory and back to its parent.
- Distinguish a current directory from a parent directory.

Continue with [the next lesson](/paths/0/0-2/07).
