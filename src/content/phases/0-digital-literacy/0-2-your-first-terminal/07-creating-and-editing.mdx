---
title: "Creating and Editing"
description: "mkdir, touch, cat, echo — making files from the command line."
phase: 0
module: "0-2-your-first-terminal"
order: 3
estimatedMinutes: 7
difficulty: 1
standards:
  cs2023: ["SP-1"]
  swebok: ["Software Engineering Tools and Methods"]
  bloom: "understand"
  sfia: 2
  dreyfus: "novice"
vocabulary: ["terminal"]
prerequisites: ["0/0-2/06"]
learningOutcomes:
  - "Identify commands that create directories and empty files"
  - "Distinguish overwriting a file from appending to it"
  - "Explain how cat displays saved text"
thresholdConcept: false
---

## Why this matters

Navigating is half of using a terminal. The other half is creating, reading, and modifying files. Four small commands cover the basics — and once you know them, you can scaffold an entire project without ever touching the mouse.

## Start with something familiar

Imagine an empty notebook, a shelf for notebooks, and two choices: replace a page or add another line.

## The mental model

> Directories organize files; commands can create or display files, while redirection chooses where output is written.

## mkdir: make a directory

```text
$ mkdir notes
$ ls
notes
```

Add `-p` to create nested directories in one shot:

```text
$ mkdir -p notes/2026/april
```

The `-p` means "make parents as needed." Without it, `mkdir` errors out if any parent directory is missing.

## touch: create an empty file

```text
$ touch ideas.md
$ ls
ideas.md  notes
```

`touch` was originally designed to update file timestamps; the side effect of creating a missing file is how everyone uses it now.

## cat: read a file

```text
$ cat README.md
# My Project
A small project to learn the terminal.
```

`cat` ("concatenate") prints the contents of one or more files to your terminal. For very long files, use `less` instead — it lets you scroll page by page and quit with `q`.

## echo and redirection: write a file

`echo` prints text. The `>` operator redirects that text into a file.

```text
$ echo "Hello, world." > hello.txt
$ cat hello.txt
Hello, world.
```

A single `>` overwrites. A double `>>` appends.

```text
$ echo "Second line." >> hello.txt
$ cat hello.txt
Hello, world.
Second line.
```

You wrote a two-line file without ever opening an editor.

## Guided practice

A file contains First. Predict its contents after echo "Second" >> file, then compare using > instead.

Predict on paper or in your head before opening the answer; no installation or internet connection is needed.

<details>
  <summary>Reveal the practice answer</summary>

Appending keeps First and adds Second on the next line. Overwriting replaces the old contents with Second.

</details>

<FillBlank
  prompt="To make a directory: ___. To make an empty file: ___. To print a file: ___."
  answers={["mkdir", "touch", "cat"]}
  hints={["Five letters.", "Five letters.", "Three letters."]}
/>

## Check your understanding

<Quiz
  questions={[
    {
      type: "multiple-choice",
      question: "What does `>` do when used after a command?",
      options: [
        "Pipes output to another command",
        "Redirects output into a file, overwriting it",
        "Appends output to a file",
        "Compresses the output",
      ],
      correct: 1,
      explanation: "Single `>` overwrites. Double `>>` appends.",
    },
    {
      type: "multiple-choice",
      question: "Which command creates a missing parent directory along with its child?",
      options: ["mkdir", "mkdir -p", "touch", "cd"],
      correct: 1,
      explanation: "The -p flag tells mkdir to create any missing parents.",
    },
  ]}
/>



## What you learned

- Identify commands that create directories and empty files.
- Distinguish overwriting a file from appending to it.
- Explain how cat displays saved text.

Continue with [the next lesson](/paths/0/0-2/08).
