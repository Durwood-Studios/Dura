---
title: "What Is a Terminal?"
description: "GUI vs CLI, why developers live in terminals, and how to open your first one."
phase: 0
module: "0-2-your-first-terminal"
order: 1
estimatedMinutes: 7
difficulty: 1
standards:
  cs2023: ["SP-1"]
  swebok: ["Software Engineering Tools and Methods"]
  bloom: "understand"
  sfia: 2
  dreyfus: "novice"
vocabulary: ["terminal", "shell"]
prerequisites: []
learningOutcomes:
  - "Distinguish a terminal from the shell inside it"
  - "Explain why repeatable text commands help with repeated work"
  - "Recognize the command and displayed output in a terminal transcript"
thresholdConcept: false
---

## Why this matters

If you've only ever used a computer through windows, icons, and clicks, the terminal looks like a step backward. It isn't. It's a step toward _control_.

Every senior developer you'll meet uses one daily. They aren't doing it to look cool. They're doing it because typing one line is faster than clicking through five menus, and because terminal commands compose in ways graphical apps never can.

## Start with something familiar

Imagine a service desk: the window lets you communicate, while a person behind it interprets the request.

## The mental model

> The terminal is the communication interface; the shell interprets commands and asks programs to perform work.

## What it actually is

A terminal is a text-based interface to your operating system. You type a command, it runs, you see the result. That's it.

Behind the scenes, the terminal is talking to a **shell** — a program whose job is to read what you typed, figure out what to do, and run it. On macOS and Linux, the most common shell is `bash` or `zsh`. On Windows, it's PowerShell or `cmd.exe` (or `bash` if you install WSL).

```text
$ echo "hello world"
hello world
```

The `$` is called the **prompt**. Prompts vary by shell. In this transcript, type only echo "hello world", not the $ character. echo displays the supplied text.

## Why type instead of click?

Three reasons.

**Speed.** Once you know the commands, you'll move through your filesystem faster than any file explorer can keep up.

**Repeatability.** You can save commands in a file and run them again. Graphical automation also exists, but a short command can be easier to record and repeat.

**Composability.** The output of one command can become the input of the next. We'll see how in lesson 8 — it's the most powerful idea in this whole module.

## How to open one

- **macOS:** Spotlight (`Cmd+Space`), type "Terminal", hit enter.
- **Windows:** Search for "Windows Terminal" or "PowerShell" in the Start menu.
- **Linux:** `Ctrl+Alt+T` on most desktops, or look in your application menu for "Terminal".

You'll see a blinking cursor and a prompt. That's your starting point.

## Guided practice

In the transcript below, is hello world a second command or the result of the echo command?

Predict on paper or in your head before opening the answer; no installation or internet connection is needed.

<details>
  <summary>Reveal the practice answer</summary>

It is displayed output. The line after the prompt contains the command; the following line is its result.

</details>

<FillBlank
  prompt="The text-based interface to your OS is the ___. The program reading your commands is the ___."
  answers={["terminal", "shell"]}
  hints={["The window with the blinking cursor.", "Often bash, zsh, or PowerShell."]}
/>

## Check your understanding

<Quiz
  questions={[
    {
      type: "multiple-choice",
      question: "What is a shell?",
      options: [
        "A protective layer around a CPU",
        "A program that reads commands and runs them",
        "A type of hard drive",
        "A graphical file browser",
      ],
      correct: 1,
      explanation: "The shell is the program that turns your typed commands into actions.",
    },
    {
      type: "true-false",
      question:
        "True or false: repeatability is one reason developers use terminals.",
      options: ["True", "False"],
      correct: 0,
      explanation:
        "A saved command can be repeated without reconstructing a sequence of interactions.",
    },
  ]}
/>



## What you learned

- Distinguish a terminal from the shell inside it.
- Explain why repeatable text commands help with repeated work.
- Recognize the command and displayed output in a terminal transcript.

Continue with [the next lesson](/paths/0/0-2/06).
