---
title: "The CPU Cycle"
description: "Fetch, decode, execute — the loop that powers every computation."
phase: 0
module: "0-1-how-computers-think"
order: 4
estimatedMinutes: 8
difficulty: 1
standards:
  cs2023: ["AR-3"]
  swebok: ["Computing Fundamentals"]
  bloom: "understand"
  sfia: 2
  dreyfus: "novice"
vocabulary: ["cpu"]
prerequisites: ["0/0-1/03"]
learningOutcomes:
  - "Order the fetch, decode, and execute steps"
  - "Explain how instructions build larger program behavior"
  - "Explain why clock speed alone cannot rank CPU performance"
thresholdConcept: false
---

## Why this matters

Every program — every game, every browser tab, every calculator — eventually becomes a stream of tiny instructions running on a CPU. Understanding the loop the CPU runs is the difference between thinking of "the computer" as magic and thinking of it as a very fast, very simple machine doing the same three steps a billion times a second.

## Start with something familiar

Imagine reading the next recipe instruction, deciding what it asks, then performing it. Repeat for the next instruction.

## The mental model

> The CPU repeatedly retrieves an instruction, interprets it, and performs work; clock ticks are not a direct count of completed useful tasks.

## The loop

A CPU does three things in a tight loop:

1. **Fetch** — grab the next instruction from memory.
2. **Decode** — figure out what the instruction means.
3. **Execute** — do the thing.

Then it does it again. And again. And again. A modern CPU does this billions of times per second.

```text
fetch  → decode → execute
  ↑                  ↓
  └──────────────────┘
```

That's it. That is the entire job of a CPU. Everything else — the operating system, your apps, this lesson — is built out of that loop.

## Clock speed isn't everything

A CPU's "clock speed" is how many cycles per second it can run. 3 GHz means 3 billion cycles per second.

But more GHz doesn't always mean faster overall. A modern 3 GHz CPU is wildly faster than a 3 GHz CPU from 2005, because each cycle now does more work, and the CPU runs many instructions in parallel.

When somebody compares CPUs by clock speed alone, they're missing most of the picture.

## Guided practice

A processor has retrieved an instruction but has not interpreted it. Which stage comes next?

Predict on paper or in your head before opening the answer; no installation or internet connection is needed.

<details>
  <summary>Reveal the practice answer</summary>

Decode comes next, followed by execute. Fetch has already happened.

</details>

<FillBlank
  prompt="The CPU repeatedly performs three steps: ___, decode, and ___."
  answers={["fetch", "execute"]}
  hints={["Get the next instruction.", "Actually do what the instruction says."]}
/>

## Check your understanding

<Quiz
  questions={[
    {
      type: "multiple-choice",
      question: "What are the three steps in a CPU cycle?",
      options: [
        "Read, write, store",
        "Fetch, decode, execute",
        "Open, modify, close",
        "Plan, execute, verify",
      ],
      correct: 1,
      explanation: "Fetch the next instruction, decode what it means, then execute it. Repeat.",
    },
    {
      type: "multiple-choice",
      question: "Why can two CPUs at the same clock speed have very different performance?",
      options: [
        "Clock speed is a marketing lie",
        "One has more memory",
        "Each cycle can do different amounts of work, and modern CPUs run many instructions in parallel",
        "They use different operating systems",
      ],
      correct: 2,
      explanation:
        "Modern CPUs use pipelining, superscalar execution, and other tricks to do multiple operations per cycle.",
    },
  ]}
/>



## What you learned

- Order the fetch, decode, and execute steps.
- Explain how instructions build larger program behavior.
- Explain why clock speed alone cannot rank CPU performance.

Continue with [the next lesson](/paths/0/0-2/05).
