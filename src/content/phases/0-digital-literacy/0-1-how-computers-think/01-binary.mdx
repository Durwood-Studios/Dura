---
title: "Binary: The Language of Machines"
description: "How computers represent everything as 1s and 0s — and why."
phase: 0
module: "0-1-how-computers-think"
order: 1
estimatedMinutes: 8
difficulty: 1
standards:
  cs2023: ["AR-1"]
  swebok: ["Computing Fundamentals"]
  bloom: "understand"
  sfia: 2
  dreyfus: "novice"
vocabulary: ["bit", "byte", "binary", "ascii"]
prerequisites: []
learningOutcomes:
  - "Convert a small binary number to decimal"
  - "Explain why eight bits represent 256 values"
  - "Explain how a character encoding maps numbers to letters"
thresholdConcept: false
---

## Why this matters

Every photo you've ever seen on a screen, every emoji in a message, every song streaming through your headphones is, underneath, a long string of <VocabTooltip slug="bit">bits</VocabTooltip>. If you understand binary, you understand the floor that everything else stands on.

You will not need to do binary math in your day job. You will need to understand it the day a bug stops making sense, and the answer turns out to be that the computer was counting in twos when you assumed it was counting in tens.

## A switch and a number

Imagine a single light switch. It is either off or on. That is one bit of information.

We write "off" as `0` and "on" as `1`. That is the entire alphabet a computer speaks.

## The mental model

> A **bit** is a single 0 or 1. Eight bits make a **byte**, which can represent 256 different values. That is enough to cover every letter on a keyboard, with room to spare.

## Counting in twos

In the number system you grew up with — base 10 — each column is worth ten times the one to its right: ones, tens, hundreds, thousands.

Binary works the same way, but each column is worth two times the one to its right: ones, twos, fours, eights, sixteens.

So `1010` in binary is:

```text
1 × 8 = 8
0 × 4 = 0
1 × 2 = 2
0 × 1 = 0
        ─
        10
```

That is the number ten, written using only switches.

## From numbers to letters

If a byte can hold 256 values, you can map each value to a letter. That is exactly what <VocabTooltip slug="ascii">ASCII</VocabTooltip> does. The capital letter `A` is the number 65. Lowercase `a` is 97. The digit `0` (as a character) is the number 48.


Pictures, sound, and video all work the same way. They are numbers, stored as bits, decoded by software that knows how to interpret them.

<TryInDiscovery
  href="/discover/secret-codes/binary-painter"
  label="Binary Painter"
  description="Flip eight binary switches and watch a color appear. The same bits, the same encoding, hands-on instead of read-about."
/>

## Guided practice

What decimal number is binary 0101? Explain which columns contribute.

Predict on paper or in your head before opening the answer; no installation or internet connection is needed.

<details>
  <summary>Reveal the practice answer</summary>

5: the four and one columns are on; the eight and two columns contribute zero.

</details>

<FillBlank
  prompt="A single binary digit is called a ___. Eight of them grouped together form a ___."
  answers={["bit", "byte"]}
  hints={["The smallest unit of data.", "256 possible values."]}
/>

## Check your understanding

<Quiz
  questions={[
    {
      question: "How many different values can a single byte represent?",
      options: ["8", "16", "128", "256"],
      correct: 3,
      explanation: "Eight bits, each with two possible states, gives 2^8 = 256.",
    },
    {
      question: "What is the number 6 in binary?",
      options: ["011", "100", "110", "101"],
      correct: 2,
      explanation:
        "6 = 4 + 2 + 0, so the bits in the 4s, 2s, and 1s columns are 1, 1, 0 — that is 110.",
    },
  ]}
/>




## What you learned

- Convert a small binary number to decimal.
- Explain why eight bits represent 256 values.
- Explain how a character encoding maps numbers to letters.

Continue with [the next lesson](/paths/0/0-1/02).
