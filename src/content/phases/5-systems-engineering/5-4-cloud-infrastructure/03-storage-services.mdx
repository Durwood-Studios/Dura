---
title: "Storage Services: Object, Block, and File"
description: "Learn the three cloud storage models — object, block, and file — when to use each, and how object storage works under the hood."
phase: 5
module: "5-4-cloud-infrastructure"
order: 3
difficulty: 3
bloom: "apply"
vocabulary: ["object-storage","block-storage","s3","ebs","key-value-store"]
standards: {"cs2023":["SF-1"],"swebok":["Software Engineering Tools and Methods"],"bloom":"apply","sfia":4,"dreyfus":"competent"}
estimatedMinutes: 15
prerequisites: ["5/5-4/02"]
thresholdConcept: false
learningOutcomes: ["Match each workload to an appropriate storage interface","Identify concrete semantics to verify","Separate primary storage from backup and recovery"]
---

## Why this matters

Data outlives code. An application you wrote five years ago might be rewritten in a completely different language today, but the data it generated still needs to be accessible, durable, and retrievable. Cloud providers offer three fundamentally different storage abstractions — object, block, and file storage — each designed for a different category of use case.

Understanding the differences is not academic: choosing the wrong storage type can result in an architecture that doesn't scale, costs dramatically more than necessary, or simply doesn't work for your access patterns.

## Start with something familiar

A numbered parcel, a raw disk, and a shared filing cabinet expose different ways to retrieve and modify information.

## The mental model

> Object storage retrieves named objects, block storage exposes blocks to a storage stack, and file storage exposes filesystem operations. Similar-looking paths do not imply identical rename, append, locking, or transaction semantics.

## Object Storage

**Object storage** stores data as discrete objects in a flat namespace. Each object has a unique key (a string identifier), a blob of data (bytes of any size), and metadata (key-value pairs describing the object). There are no directories, no hierarchies, no random-access reads to byte offsets. You interact with objects through simple operations: `put`, `get`, `delete`, and `list`.

**Amazon S3** (Simple Storage Service) is the canonical example of object storage. An S3 object lives in a bucket, is identified by its key, and can be retrieved via HTTP. A key like `images/user-avatars/12345.png` looks like a path, but it isn't — S3 has no filesystem. The slash is just part of the key string; S3 lets you filter by prefix to simulate directory-like behavior.

Object storage is designed for three things: massive scale, high durability, and cheap cost. S3 is designed to survive the loss of an entire availability zone without data loss. Under the hood, your objects are replicated across at least three AZs within a region. AWS quotes 99.999999999% (eleven nines) durability — meaning if you store ten million objects, you might expect to lose one over 10,000 years.

Object storage is the right choice for: static assets (images, videos, CSS, JavaScript bundles), backups and archives, data lake raw files, log aggregation, and any file that is written once and read many times. It is the wrong choice for anything that needs random byte-level writes, low-latency random reads, or filesystem semantics (like a database's data files).

## Block Storage

**Block storage** exposes raw storage as a device — similar to a hard drive connected to your VM. You format it with a filesystem, and your OS can read and write arbitrary byte ranges at any offset. It behaves exactly like local disk but lives on the network.

**Amazon EBS** (Elastic Block Store) is the AWS block storage product. An EBS volume attaches to exactly one EC2 instance at a time (with one exception: `io2` volumes support multi-attach in limited scenarios). It is persistent — data survives a VM reboot or termination as long as you don't delete the volume.

Block storage is the right choice for databases, operating system boot volumes, any workload that needs low-latency random reads/writes, and applications that require filesystem semantics. Most managed databases (RDS, self-hosted PostgreSQL) run on block storage under the hood.

The trade-off: block storage is more expensive than object storage per gigabyte, and it doesn't scale horizontally — you can't easily stripe across hundreds of volumes without specialized software.

## File Storage

Cloud **file storage** (AWS EFS, Azure Files, Google Filestore) provides a network filesystem that multiple instances can mount simultaneously. If your architecture has ten application servers that all need to read and write to the same shared directory, file storage solves that problem cleanly. Neither object storage nor block storage supports concurrent multi-instance access with POSIX semantics.

File storage is typically used for: shared configuration, content management systems where multiple web servers need the same media files, or any workflow using tools that assume a POSIX filesystem.

## Choosing the Right Storage

| Type        | Access Pattern                 | Cost     | Durability     | Use Case                    |
| ----------- | ------------------------------ | -------- | -------------- | --------------------------- |
| Object (S3) | Key-value get/put              | Very low | Extremely high | Assets, backups, data lakes |
| Block (EBS) | Random byte-level              | Medium   | High           | Databases, boot volumes     |
| File (EFS)  | POSIX filesystem, multi-client | Higher   | High           | Shared app storage          |

The most common mistake: using a database to store binary blobs (images, videos, documents). Databases are optimized for structured queries, not large binary transfers. The correct pattern is to store the blob in S3 and store the S3 key in the database.



## Guided practice

Can an application assume an object key with slashes supports every POSIX directory operation?

<details>
<summary>Reveal the reasoning checkpoint</summary>

No. A key prefix is not proof of filesystem rename, locking, or append semantics.

</details>

## Check your understanding

<WrittenExercise
 instructions={"Apply these storage models to three needs: immutable uploaded photographs, a database volume requiring supported filesystem and durability behavior, and a shared directory used by several workers. Choose a candidate for each and write the exact behavior you must verify before deployment."}
 rubric={["Matches each workload to an appropriate storage interface","Identifies concrete semantics to verify","Separates primary storage from backup and recovery"]}
 modelAnswer={"Use object storage as a candidate for photographs, verifying object access policy, overwrite/version behavior, and lifecycle rules. Use a database-supported block-volume and filesystem configuration for the database, verifying flush semantics, attachment constraints, backups, and recovery. Use supported shared file storage for the shared directory, verifying concurrent access and locking semantics. None of these choices alone supplies a tested backup or makes every application compatible."}
/>

<Quiz
  question="A developer stores user-uploaded profile pictures directly in a PostgreSQL database as binary blobs. What is the primary problem with this approach?"
  options={[
    "PostgreSQL does not support binary data types",
    "Databases are optimized for structured queries, not large binary transfers; this hurts query performance and costs more per GB than object storage",
    "Binary blobs stored in a database cannot be served over HTTP",
    "The images will not be replicated across availability zones",
  ]}
  answer={1}
  explanation="Databases incur significant overhead reading and writing large binary blobs. Every image retrieval goes through the database's query engine, consuming connection pool slots and memory. Object storage (like S3) is purpose-built for large binary data, is dramatically cheaper per GB, and can serve files directly over HTTP. The correct pattern: store blobs in S3, store the S3 key in the database."
/>

<Quiz
  question="You need a storage solution for a distributed web application where ten EC2 instances must all read and write to the same shared directory of configuration files simultaneously. Which storage type is appropriate?"
  options={[
    "EBS — because it offers the lowest latency",
    "S3 — because it supports concurrent access",
    "EFS (file storage) — because it provides a shared POSIX filesystem mountable by multiple instances",
    "Local instance storage — because it is fastest for frequent writes",
  ]}
  answer={2}
  explanation="EBS attaches to only one instance at a time. S3 does not support POSIX filesystem semantics (no directories, no random-offset writes). EFS provides a shared network filesystem that multiple instances can mount simultaneously with full POSIX compatibility — exactly what this pattern requires."
/>

<FillBlank
  question="In object storage, each piece of data is addressed by a unique string called a ___, and retrieved with a simple get operation rather than filesystem path traversal."
  answer="key"
  hints={["Object storage is essentially a ___ -value store for blobs."]}
/>

## What you learned

- Match each workload to an appropriate storage interface
- Identify concrete semantics to verify
- Separate primary storage from backup and recovery
