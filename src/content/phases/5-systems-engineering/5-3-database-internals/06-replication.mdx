---
title: "Replication: Leader-Follower, Quorums, and Consistency"
description: "Understand how databases replicate data across multiple nodes, the tradeoffs between replication strategies, and what 'eventual consistency' actually means in practice."
phase: 5
module: "5-3-database-internals"
order: 6
difficulty: 3
bloom: "evaluate"
vocabulary: ["replication","leader","follower","replication-lag","quorum"]
standards: {"cs2023":["IM-1"],"swebok":["Software Design"],"bloom":"evaluate","sfia":5,"dreyfus":"proficient"}
estimatedMinutes: 15
prerequisites: ["5/5-3/05"]
thresholdConcept: false
learningOutcomes: ["Identify the stale follower result","Compare read routing with acknowledgment policy","Defend a policy with explicit latency and failover assumptions"]
professionalContext: "Evaluate a leader that acknowledges locally while its follower remains at version 8 and the leader reaches version 9. Compare reading the follower, reading the leader, and requiring a replica acknowledgment. Defend a policy for read-after-write behavior and explain the latency and failover assumptions."
---

## Why this matters

A single database server is a single point of failure. If that machine crashes, loses power, or has a hardware failure, your application goes down and potentially loses data. Replication solves this by maintaining copies of the database on multiple servers, so that the failure of any single node is survivable.

Replication serves three purposes simultaneously: **fault tolerance** (the system keeps running when a node fails), **read scalability** (read queries can be distributed across replicas), and **low-latency reads** (replicas can be placed geographically close to users in different regions).

The cost of replication is complexity. Now you have multiple copies of data that must stay (or at least eventually become) consistent. Changes made to one node must propagate to others. What happens if a network partition prevents propagation? These questions define the landscape of replication strategies.

## Start with something familiar

A receipt from one archive does not prove that a second archive already has the same revision.

## The mental model

> Replication acknowledgments and read routing define which failures and stale reads a system can tolerate. Quorum arithmetic needs explicit overlap and protocol assumptions; it does not independently prove linearizability.

## Leader-follower replication

The dominant pattern for relational databases is **leader-follower** (also called primary-replica or master-slave). One node is designated the **leader** — the only node that accepts writes. All other nodes are **followers** — they receive a stream of changes from the leader and apply them in the same order.

In PostgreSQL, this stream is the WAL. Every change written to the leader's WAL is shipped to followers, which replay it. The followers maintain an exact copy of the leader's data at some point in the past. The gap between the leader's current state and what followers have applied is the **replication lag**.

**Synchronous replication** waits for the configured standby acknowledgment policy. PostgreSQL distinguishes receiving, durable flushing, and applying WAL; receipt or flush does not necessarily mean a standby query can already see the change. Waiting adds latency and depends on standby availability. Review the exact [standby and synchronous-commit configuration](https://www.postgresql.org/docs/current/warm-standby.html) before stating its guarantee.

**Asynchronous replication** (the default) lets the leader commit and return immediately. Followers catch up in the background. This keeps write latency low but means followers might be behind by milliseconds, seconds, or (under heavy load or network issues) longer. If the leader crashes before followers catch up, those writes may be lost.

**Failover:** When the leader fails, a follower must be promoted to become the new leader. The promoted follower becomes the new write destination, and other followers repoint to it. The catch: if the promoted follower was lagging, it might be missing some writes that the old leader acknowledged. This is why synchronous replication is critical for systems where data loss is unacceptable.

## Multi-leader replication

In multi-leader (or multi-primary) replication, multiple nodes accept writes independently. This enables writes to continue even if one datacenter is partitioned from others. The tradeoff: when two leaders accept writes to the same row concurrently, a **conflict** arises. Someone has to resolve it.

Conflict resolution strategies include: last-write-wins (the write with the later timestamp survives — simple but loses data), merge (combine both updates somehow — works for counters and sets, not for arbitrary edits), and application-defined resolution (the application receives the conflict and decides — most flexible, most complex).

Multi-leader replication is used by some NoSQL databases and by systems designed for geographic distribution, but it is unusual in traditional relational databases because conflict resolution is genuinely hard and the semantics are difficult to reason about.

## Quorum reads and writes

Quorum-based systems (used by Cassandra, DynamoDB, and distributed databases built on Paxos/Raft) don't have a single leader for all operations. Instead, they define a **quorum** — the minimum number of nodes that must acknowledge a read or write before it is considered successful.

In a fixed set of N replicas, W + R > N means every read set of size R intersects every write set of size W. This intersection is useful, but consistency also depends on version ordering, concurrent-write handling, successful-write definitions, failure behavior, and whether the protocol actually uses those fixed replica sets. Intersection arithmetic alone does not prove linearizability.

**Replication lag** in quorum systems is bounded by the read quorum — you read from enough nodes that at least one has the latest data. But with eventually consistent systems (R = 1, W = 1), you may read stale data from a node that hasn't received the latest write yet.

## What eventual consistency actually means

"Eventual consistency" means that, in the absence of further updates, all replicas will _eventually_ converge to the same value. It says nothing about _how long_ "eventually" takes, and it does not guarantee what you read in the meantime.

For user-facing applications, eventual consistency can cause jarring experiences: a user submits a form, the write goes to one replica, the page refreshes and the GET request hits a different replica that hasn't received the write yet — the update appears to disappear. "Read-your-own-writes" consistency is a weaker guarantee that specifically addresses this: a user always sees the effects of their own writes, even if other users might temporarily not.

**Linearizability** requires operations to appear atomic in an order that respects real-time completion. A completed write must be reflected by a later read unless superseded by another write. Leader routing and quorum protocols need correct coordination and failover behavior to provide it; merely having a leader or overlapping quorums is insufficient.



## Guided practice

Can a write acknowledged only by the leader be absent from a lagging follower immediately afterward?

<details>
<summary>Reveal the reasoning checkpoint</summary>

Yes. Local acknowledgment does not imply that asynchronous replication has completed.

</details>

## Check your understanding

<WrittenExercise
 instructions={"Evaluate a leader that acknowledges locally while its follower remains at version 8 and the leader reaches version 9. Compare reading the follower, reading the leader, and requiring a replica acknowledgment. Defend a policy for read-after-write behavior and explain the latency and failover assumptions."}
 rubric={["Identifies the stale follower result","Compares read routing with acknowledgment policy","Defends a policy with explicit latency and failover assumptions"]}
 modelAnswer={"A follower read can return version 8 until replication catches up. Routing the subsequent read to the still-current leader can provide read-after-write behavior under that routing and leadership assumption. Waiting for an appropriate replica acknowledgment improves the stated durability or visibility boundary but adds latency and can reduce availability when that replica is unavailable. Define which acknowledgment is required, which replica may serve reads, and how promotion excludes nodes missing acknowledged writes. Synchronous replication alone is not a blanket guarantee against every failure or stale read."}
/>

<Quiz
  question="In synchronous leader-follower replication, what happens to write latency compared to asynchronous replication?"
  options={[
    "Write latency decreases because the leader doesn't wait for the data page write",
    "Write latency increases because the leader waits for at least one follower to acknowledge the WAL record",
    "Write latency is unchanged — the synchronous vs asynchronous distinction only affects failover behavior",
    "Write latency is non-deterministic because followers apply writes in batches",
  ]}
  answer={1}
  explanation="Waiting for a configured replica acknowledgment adds communication and processing delay. The durability and visibility guarantees depend on what acknowledgment is required and which replica can be promoted; synchronous replication is not a promise of zero loss under every failure."
/>

<Quiz
  question="A quorum system has 5 nodes and requires a write quorum of 3 (W=3). For every read set to intersect every write set, the minimum read quorum R must be:"
  options={[
    "1 (read from any one node)",
    "2 (read from any two nodes)",
    "3 (read from any three nodes)",
    "5 (read from all nodes)",
  ]}
  answer={2}
  explanation="Set intersection requires W + R greater than N. With N=5 and W=3, R must be at least 3. This proves an overlap of the selected sets, not a complete consistency guarantee; ordering, failures, and concurrent writes still require a correct protocol."
/>

<FillBlank
  question="The delay between when the leader commits a write and when a follower applies that write is called ___."
  answer="replication lag"
  hints={["It describes how far 'behind' a follower is relative to the leader"]}
/>

## What you learned

- Identify the stale follower result
- Compare read routing with acknowledgment policy
- Defend a policy with explicit latency and failover assumptions
