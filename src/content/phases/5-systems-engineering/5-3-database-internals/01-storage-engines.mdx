---
title: "Storage Engines: B-Trees vs LSM-Trees"
description: "Understand how databases physically organize data on disk, and why the choice between B-trees and LSM-trees determines a database's read vs write performance profile."
phase: 5
module: "5-3-database-internals"
order: 1
difficulty: 3
bloom: "analyze"
vocabulary: ["b-tree","lsm-tree","storage-engine","compaction","write-amplification"]
standards: {"cs2023":["IM-1"],"swebok":["Software Design"],"bloom":"analyze","sfia":5,"dreyfus":"proficient"}
estimatedMinutes: 15
prerequisites: ["4/4-3/07", "3/3-4/04"]
thresholdConcept: false
learningOutcomes: ["Compare the actual write paths","Distinguish logical ordering from physical layout","Identify measurements that test the recommendation"]
---

## Why this matters

When you write `INSERT INTO users (name) VALUES ('Alice')`, you probably assume that row lands somewhere on disk. But _how_ it lands — in what data structure, in what order, with what overhead — is entirely the storage engine's job. The choice of storage engine determines whether your database is fast at reads, fast at writes, or somewhere in between. It is one of the most consequential decisions in database design.

Two families of storage engines dominate the industry: **B-trees** (used by PostgreSQL, MySQL InnoDB, SQLite) and **LSM-trees** (used by RocksDB, Cassandra, LevelDB, and increasingly by newer systems like CockroachDB). They make radically different tradeoffs.

## Start with something familiar

A sorted filing cabinet updates individual drawers; a notebook records new entries quickly and periodically merges them into an ordered archive.

## The mental model

> B-tree and LSM designs make different read, write, and space tradeoffs. Logical key order does not guarantee physically contiguous storage, and compaction shifts rather than eliminates work.

## B-trees: the read-optimized classic

A B-tree is a balanced ordered search structure with multiple keys and child pointers per node. High fan-out can keep tree height small, but the number of page accesses depends on the tree, record representation, and cache state. Logical key order does not ensure physically adjacent pages.

B-tree nodes correspond to fixed-size disk pages (typically 4KB or 8KB). When you insert a row, the engine finds the correct leaf page and writes the new key in sorted position. If the page is full, it splits into two pages and updates the parent node to point to both. This split can cascade up the tree, but it is rare in practice.

**The write tradeoff:** Updates can dirty pages and splits can affect several nodes. Page sizes and update strategies vary by engine; buffered writes need not cause a separate immediate physical write for each logical change. WAL is a common crash-recovery design, while other storage architectures use different durability protocols.

**The read advantage:** Because B-tree pages are always in sorted order and stored in a stable location on disk, range scans are highly efficient. Reading all users with IDs between 1000 and 2000 means finding the leaf page for ID 1000 and then scanning forward — the pages are physically adjacent (or nearly so) on disk.

## LSM-trees: the write-optimized challenger

An LSM-tree — Log-Structured Merge-tree — takes a completely different approach. Writes never modify existing data. Instead, every write is appended to an in-memory buffer called a **memtable**, which is also mirrored in an append-only log on disk for crash safety. When the memtable fills up, it is flushed to disk as an immutable, sorted file called an **SSTable** (Sorted String Table).

This means every write is a sequential append — the fastest possible disk operation. There is no hunting for the right page to modify. You simply tack new data onto the end. This is why LSM-tree databases can sustain dramatically higher write throughput than B-tree databases under heavy write load.

**The read penalty:** Data for a single key might be spread across the memtable, multiple levels of SSTables, and the most recent SSTable that supersedes older ones. To read a key, the engine must check all of these in sequence, from newest to oldest, until it finds the key. In the worst case, this touches many files. Bloom filters (probabilistic data structures) help skip SSTables that definitely don't contain the key, but reads are still more expensive than in a B-tree.

**Compaction:** Left unchecked, SSTables accumulate indefinitely, making reads slower and consuming more disk. **Compaction** is the background process that merges older SSTables together, discarding deleted and overwritten data. Compaction keeps read performance bounded but introduces its own cost: the engine must read and rewrite data that might have already been written once. This is **write amplification** — the ratio of actual bytes written to disk versus logical bytes written by the application. A write amplification factor of 10 means a 1MB logical write causes 10MB of actual disk I/O.

## Choosing the right engine

The choice depends on your workload:

| Workload                                 | Choose                                    |
| ---------------------------------------- | ----------------------------------------- |
| Heavy reads, moderate writes             | B-tree (PostgreSQL, MySQL)                |
| Write-heavy, time-series, event logs     | LSM-tree (RocksDB, Cassandra)             |
| Mixed, needs SQL                         | B-tree (battle-tested, mature)            |
| Mobile / embedded with high write bursts | LSM-tree (SQLite in WAL mode is a hybrid) |

Modern databases blur these lines. PostgreSQL's heap storage is a B-tree variant, but it borrows LSM ideas (like the WAL). RocksDB allows tunable compaction strategies. Understanding the tradeoffs means you can make informed decisions when picking a database and tuning its configuration.



## Guided practice

Does an LSM tree eliminate the cost of rewriting stored data?

<details>
<summary>Reveal the reasoning checkpoint</summary>

No. Compaction merges and rewrites runs; batching changes when and how that cost occurs.

</details>

## Check your understanding

<WrittenExercise
 instructions={"Compare storage designs for a service with sustained writes and occasional key lookups versus a service dominated by short range scans. Analyze memtable flushes, compaction, read amplification, and page updates. State which measurements could overturn your initial choice."}
 rubric={["Compares the actual write paths","Distinguishes logical ordering from physical layout","Identifies measurements that test the recommendation"]}
 modelAnswer={"An LSM design is a candidate for sustained writes because it buffers and batches sorted runs, but compaction consumes I/O and multiple runs can increase read work. A B-tree is a candidate for range-heavy reads because its ordered search structure supports locating and traversing keys, though physical placement, cache behavior, and fragmentation affect actual I/O. Neither choice follows from the label alone. Benchmark representative key distributions, tail latency, write amplification, storage use, and compaction or page-split behavior under realistic load."}
/>

<Quiz
  question="Why do B-tree databases require a write-ahead log (WAL)?"
  options={[
    "To speed up sequential writes by batching them",
    "To ensure atomicity of multi-page writes that could be interrupted by a crash",
    "To compress data before writing it to disk",
    "To support multiple concurrent writers without locks",
  ]}
  answer={1}
  explanation="B-tree writes can span multiple pages (for example, a node split updates the split page, a new sibling page, and the parent). If the system crashes mid-operation, the database could be left inconsistent. The WAL records the intended changes first, so recovery can replay or undo them to restore a consistent state."
/>

<Quiz
  question="Which workload benefits most from an LSM-tree storage engine?"
  options={[
    "A reporting database that runs complex analytical queries over many rows",
    "An online store product catalog with infrequent updates and many reads",
    "An IoT sensor platform ingesting millions of time-stamped events per second",
    "A database that serves primary-key lookups for user profile pages",
  ]}
  answer={2}
  explanation="LSM-trees excel at write-heavy workloads because every write is a sequential append. An IoT platform ingesting millions of events per second benefits enormously from this. The other options involve read-heavy or mixed workloads where B-trees' O(log n) lookup and stable page layout win."
/>

<Quiz
  question="Write amplification in an LSM-tree means:"
  options={[
    "Writes are duplicated across multiple replicas for redundancy",
    "The WAL doubles every write, causing 2x disk usage",
    "Compaction rewrites data that was already written once, so actual disk I/O exceeds logical write volume",
    "Large values are split across multiple SSTables, amplifying storage consumption",
  ]}
  answer={2}
  explanation="Write amplification is the ratio of bytes written to disk versus bytes written by the application. Compaction merges SSTables, which requires reading and rewriting existing data. A write amplification factor of 10 means writing 1MB of application data causes 10MB of actual disk writes."
/>

<FillBlank
  question="The background process in an LSM-tree database that merges older SSTables together to keep read performance bounded is called ___."
  answer="compaction"
  hints={["It 'compacts' multiple sorted files into fewer, larger ones"]}
/>

## What you learned

- Compare the actual write paths
- Distinguish logical ordering from physical layout
- Identify measurements that test the recommendation
