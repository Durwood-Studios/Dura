---
title: "Query Processing: From SQL to Results"
description: "Follow a SQL query through the parser, optimizer, and executor, and learn how cost-based optimization chooses between execution strategies."
phase: 5
module: "5-3-database-internals"
order: 3
difficulty: 3
bloom: "analyze"
vocabulary: ["query-optimizer","cost-model","execution-plan","join-order","selectivity"]
standards: {"cs2023":["IM-1"],"swebok":["Software Design"],"bloom":"analyze","sfia":5,"dreyfus":"proficient"}
estimatedMinutes: 15
prerequisites: ["5/5-3/02"]
thresholdConcept: false
learningOutcomes: ["Calculate both plan scores and crossover","Connect row-estimation error to plan selection","Distinguish model cost from measured duration"]
---

## Why this matters

When you run `SELECT * FROM orders WHERE user_id = 42 AND status = 'shipped'`, several distinct systems inside the database engine process your request before the first row is returned. Understanding these stages explains why two logically equivalent queries can have wildly different performance, and how the database chooses between them.

The pipeline has three main stages: **parsing**, **optimization**, and **execution**. Each stage transforms the query from one representation to another.

## Start with something familiar

A route planner estimates travel costs before a trip, then compares the estimate with actual conditions afterward.

## The mental model

> A query optimizer compares candidate plans using statistics and a cost model. Estimated cost is a model score, not a promise of elapsed milliseconds.

## Stage 1: Parsing and semantic analysis

The **parser** treats the SQL string as text and applies the SQL grammar to produce an **abstract syntax tree** (AST) — a tree-shaped data structure representing the query's logical structure. At this stage, the parser checks syntax only. It has no idea whether the tables or columns in your query actually exist.

**Semantic analysis** follows parsing. The analyzer resolves table and column names against the database catalog, verifies that joins are between compatible columns, applies type checking, and expands `*` into a concrete column list. The result is a **logical plan** — a tree of relational algebra operations (Select, Project, Join, Sort, etc.) that describes _what_ to compute, not _how_ to compute it.

At this point, all equivalent reformulations of the query (`WHERE a = 1 AND b = 2` vs `WHERE b = 2 AND a = 1`) produce identical logical plans. The optimizer's job is to choose the best physical implementation.

## Stage 2: The query optimizer

The **query optimizer** is arguably the most sophisticated component in a relational database. Its job is to take the logical plan and choose an **execution plan** — a concrete sequence of physical operations — that minimizes execution cost. Two decisions dominate query optimization: choosing access paths (sequential scan vs. index scan) and choosing join order.

### Cost models and selectivity

The optimizer uses a **cost model** — a mathematical function that estimates the CPU and I/O cost of each operation. It consults statistics stored in the catalog: table row counts, column histograms (distributions of values), and number of distinct values.

**Selectivity** is the fraction of rows a filter condition is expected to return. An equality filter on a high-cardinality column (like `user_id = 42` in a table of millions) has low selectivity — it returns very few rows. A filter on a low-cardinality column (like `status = 'active'` where 90% of rows are active) has high selectivity — most rows pass the filter.

Low selectivity means the optimizer prefers an index scan (fetch only the matching 0.001% of rows). High selectivity means a sequential scan might be cheaper (you'd visit most pages anyway via random index lookups). The optimizer computes an estimated cost for each plan alternative and picks the minimum.

### Join ordering

For a query joining three tables A, B, C, there are multiple valid join orders: `(A JOIN B) JOIN C`, `(A JOIN C) JOIN B`, `(B JOIN C) JOIN A`, and their mirror images — a total of 12 orderings for 3 tables, and n!/2 orderings for n tables. With 8 tables in a complex query, this is over 20,000 possibilities.

Real optimizers use **dynamic programming** with memoization to explore the plan space efficiently, pruning obviously expensive branches early. PostgreSQL uses this approach for small numbers of tables and falls back to a genetic algorithm for queries with many joins.

The order matters enormously. Joining the large table first can produce an intermediate result with millions of rows that must then be filtered. Joining the most selective filtered table first keeps intermediate results small.

## Stage 3: Execution

The **executor** walks the physical plan tree, calling each operator to produce rows. PostgreSQL uses a **volcano model** (also called iterator model): each operator implements a `next()` function that pulls one row from its child operators. The query result is materialized row by row from the bottom of the plan tree upward.

Physical operators include:

- **Sequential scan**: iterate over every page of the table
- **Index scan**: walk the B+ tree, fetch matching heap pages
- **Hash join**: build a hash table from the smaller relation, probe it with rows from the larger
- **Nested loop join**: for each row in the outer table, scan (or index-scan) the inner table
- **Merge join**: join two pre-sorted streams by merging them, requires both inputs sorted on join key
- **Sort**: materialize all rows, sort them (or use a top-N heap for LIMIT queries)
- **Aggregate**: compute GROUP BY results using hash aggregation or sorted aggregation



## Guided practice

If m=192, does this toy model establish that the index is faster?

<details>
<summary>Reveal the reasoning checkpoint</summary>

No. The scores tie, and modeled equality does not establish measured runtime.

</details>

## Check your understanding

<WrittenExercise
 instructions={"Compare a toy sequential-scan cost of 1000 with index cost 40 + 5m, where m is estimated matching rows. Analyze the selected plan at m=20 and m=300, calculate the crossover, and explain how an underestimate can select a poor plan."}
 rubric={["Calculates both plan scores and crossover","Connects row-estimation error to plan selection","Distinguishes model cost from measured duration"]}
 modelAnswer={"At 20 matches the index score is 140, below 1000. At 300 it is 1540, so the sequential scan wins. Equality occurs at m=192. An estimate below 192 can favor the index even if actual matches are much higher, causing more random or repeated accesses than expected. These constants describe this exercise only; real systems consider additional costs and statistics. Compare estimated and actual rows before changing indexes or forcing a plan."}
/>

<Quiz
  question="A column with 3 distinct values (status: active, inactive, deleted) in a table of 1 million rows has:"
  options={[
    "High selectivity on an equality filter — the index will almost always be used",
    "Low selectivity on an equality filter — a sequential scan is likely cheaper",
    "Undefined selectivity — the optimizer cannot estimate costs without a histogram",
    "Perfect selectivity — it returns exactly one row per filter",
  ]}
  answer={1}
  explanation="Selectivity for an equality filter on a column with 3 distinct values is roughly 1/3 — meaning about 333,000 rows match. At that fraction, accessing each via random index lookups (heap fetches) is far more expensive than a single sequential scan. Low selectivity (many matching rows) favors sequential scans."
/>

<Quiz
  question="Why does join ordering matter so much for query performance?"
  options={[
    "Some join algorithms are slower than others regardless of input size",
    "The order determines which tables use sequential scans vs index scans",
    "Joining the most selective filtered table first keeps intermediate result sets small, reducing downstream work",
    "The optimizer cannot use indexes when tables are joined in the wrong order",
  ]}
  answer={2}
  explanation="Each join produces an intermediate result set. If you join large unfiltered tables first, that intermediate set can have millions of rows that must then be filtered or joined further. Applying selective filters early and joining small intermediate results keeps the plan fast. This is why the optimizer explores many join orderings."
/>

<Quiz
  question="The volcano (iterator) model of query execution:"
  options={[
    "Materializes all intermediate results before passing them to the next operator",
    "Uses a push model where producers send rows downstream to consumers",
    "Uses a pull model where each operator calls next() on its child to get one row at a time",
    "Executes operators in parallel across multiple CPU cores",
  ]}
  answer={2}
  explanation="In the volcano model, the root operator (e.g., the client receiving results) calls next() on its child. That child calls next() on its child, and so on, pulling rows up through the plan tree one at a time. This requires minimal memory — rows are processed as they arrive rather than all materialized at once."
/>

<FillBlank
  question="The fraction of rows a filter condition is expected to return, used by the optimizer to estimate plan costs, is called ___."
  answer="selectivity"
  hints={["A filter that returns very few rows has low ___"]}
/>

## What you learned

- Calculate both plan scores and crossover
- Connect row-estimation error to plan selection
- Distinguish model cost from measured duration
