---
title: "Processes: Programs in Motion"
description: "Understand how the OS gives every running program its own isolated execution environment, tracks it in a process table, and manages its lifecycle."
phase: 5
module: "5-1-operating-systems"
order: 2
difficulty: 3
bloom: "apply"
vocabulary: ["process","pid","fork","exec","process-table"]
standards: {"cs2023":["OS-1"],"swebok":["Computing Fundamentals"],"bloom":"understand","sfia":4,"dreyfus":"advanced-beginner"}
estimatedMinutes: 15
prerequisites: ["5/5-1/01"]
thresholdConcept: false
learningOutcomes: ["Distinguish program from process instance","Separate waiting from termination","Explain why an exit record remains until reaped"]
---

## Why this matters

A program is a static artifact — a file sitting on disk containing instructions. A **process** is what happens when the operating system loads that program into memory and starts running it. The distinction matters enormously: a single program can spawn dozens of processes simultaneously, and every process gets its own isolated slice of the system's resources.

When you open two terminal windows and run `python3 script.py` in each, you have two separate processes, each with its own memory, its own file descriptors, and its own execution state — even though they're running identical code. This isolation is one of the fundamental guarantees of modern operating systems.

## Start with something familiar

A recipe file is passive until a cook starts a particular preparation with its own tools and progress.

## The mental model

> A program is executable material; a process is a running instance with resources and execution state. Termination and collection of its exit status are separate lifecycle events.

## Anatomy of a Process

Every process has several key attributes maintained by the kernel:

**Process ID (PID):** An integer identifies a process within its PID namespace while that process exists. On Linux the namespace init process has PID 1. PIDs can be reused, so a stored numeric PID alone is not a durable identity for a particular historical process.

**Parent Process ID (PPID):** Every process (except PID 1) was created by another process. The creating process is the parent, and its PID is recorded as the child's PPID. This forms a tree of processes rooted at PID 1 — the entire running state of the system is this tree.

**State:** A process is not always actively running. At any moment, a process is in one of several states:

- **Running** — currently executing on a CPU core
- **Ready (Runnable)** — able to run, but waiting for CPU time
- **Sleeping (Blocked/Waiting)** — waiting for I/O, a signal, or a timer; cannot run until the event occurs
- **Zombie** — the process has exited, but its parent hasn't yet read its exit status; the entry remains in the process table until the parent calls `wait()`
- **Stopped** — paused, usually by a signal like SIGSTOP

**Memory map:** The process's virtual address space, including its code, stack, heap, and any shared libraries.

**Open file descriptors:** References to all files, sockets, and pipes the process currently has open.

## The Process Table

The kernel maintains a data structure called the **process table** (sometimes called the task list on Linux). Every process on the system has an entry in this table containing all of its metadata: PID, PPID, state, memory maps, CPU registers, open files, and more.

On Linux, you can peek at a simplified version of this information via `/proc`. Each running process has a directory at `/proc/<pid>/` containing files that expose the process's state, memory maps, open file descriptors, environment variables, and much more.

When you run `ps aux` or `top` in a terminal, those tools are reading from the process table (via `/proc` on Linux) and formatting the output for human consumption. The data is live — it changes as processes start, run, and exit.

## How Processes Are Born: fork and exec

In Unix-like systems, there are exactly two ways a new process comes into existence: the system is booted (creating PID 1), or an existing process calls `fork()`.

**fork()** creates an almost-exact copy of the calling process. The new process (the child) gets its own copy of the parent's memory, its own PID, and starts executing at the exact same point in the code as the parent — but with `fork()` returning a different value in each. In the parent, `fork()` returns the child's PID. In the child, `fork()` returns 0. This difference is how both processes know who they are.

**exec()** (actually a family of calls like `execl`, `execv`, `execvp`) replaces the current process's memory image with a new program. The process keeps its PID and file descriptors, but its code, stack, heap, and data segments are replaced entirely with the new program's contents.

Together, fork + exec is how shells run commands. When you type `ls` at a shell prompt, the shell forks itself, and the child process calls `exec("ls")`. The shell (parent) waits for the child to complete, then prints a new prompt.

## Zombie Processes and the Wait Protocol

When a process exits, it doesn't immediately vanish. It transitions to the zombie state — its code and memory are freed, but its entry in the process table remains so the parent can read its exit code. The parent retrieves this exit code by calling `wait()`, and only then is the zombie entry removed.

If a parent exits before its children, the operating system reparents them to an appropriate reaper, which on Linux may be a subreaper or the namespace init process. Unreaped zombies retain kernel bookkeeping and an exit status, so they do consume resources even though they no longer execute user code.



## Worked task

Explain a child process lifecycle on a Unix-like system: creation, execution, exit, and parent wait/reaping. Distinguish a sleeping process from a zombie and state what information a zombie preserves.

<details>
<summary>Reveal a worked answer</summary>

A parent creates a child, which executes with its process identity and resources. A sleeping process is still live but waiting for an event. After exit, most execution resources are released, while a process-table record can remain so the parent can obtain exit status and accounting information through wait. That terminated, unreaped record is a zombie; it is not a process still running its program.

</details>

## Guided practice

Can sending more CPU time to a zombie make it finish its program?

<details>
<summary>Reveal the reasoning checkpoint</summary>

No. Its execution has already ended. The remaining issue is collecting the exit record, normally through the parent or an adopting process.

</details>

## Check your understanding

<Quiz
  question="When a Unix shell runs a command like `ls`, what sequence of operations does it use?"
  options={[
    "It replaces itself with the `ls` process using exec(), then restarts after `ls` exits",
    "It creates a new process with fork(), then the child replaces itself with `ls` using exec()",
    "It sends a message to the kernel asking it to run `ls` directly",
    "It runs `ls` in a separate thread within its own process",
  ]}
  answer={1}
  explanation="The shell forks a copy of itself (creating a child process), then the child calls exec('ls') to replace its memory image with the ls program. The parent shell waits for the child to finish using wait(), then prints a new prompt."
/>

<Quiz
  question="What is a zombie process?"
  options={[
    "A process consuming 100% CPU and making the system unresponsive",
    "A process that has exited but whose entry remains in the process table because the parent hasn't called wait()",
    "A process running malicious code in the background",
    "A process stuck in an infinite loop that cannot be killed",
  ]}
  answer={1}
  explanation="A zombie process has already finished executing — its memory is freed — but the kernel keeps its process table entry until the parent reads its exit code via wait(). Once the parent calls wait(), the entry is removed and the process is fully gone."
/>

<FillBlank
  question="Every process on a Unix system (except PID 1) has a ___ that identifies which process created it."
  answer="PPID"
  hints={["Parent ___ ID \u2014 the PID of the creating process."]}
/>

## What you learned

- Distinguish program from process instance
- Separate waiting from termination
- Explain why an exit record remains until reaped
