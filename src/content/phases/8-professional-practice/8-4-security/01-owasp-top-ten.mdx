---
title: "OWASP Top Ten"
description: "Walk through the ten most critical web application security risks, with code examples showing each vulnerability and its fix."
phase: 8
module: "8-4-security"
order: 1
estimatedMinutes: 20
difficulty: 5
standards: {"cs2023":["SE-4"],"swebok":["Software Quality"],"bloom":"analyze","sfia":5,"dreyfus":"proficient"}
vocabulary: ["owasp","injection","broken-authentication","sensitive-data-exposure","xxe","broken-access-control","security-misconfiguration","xss","insecure-deserialization","insufficient-logging"]
learningOutcomes: ["Recognize a concrete vulnerability category","Trace input to a sensitive operation","Choose a mitigation and verification test"]
prerequisites: ["8/8-3/05"]
thresholdConcept: false
professionalContext: "Apply the lesson to an operational design or review with explicit evidence and limitations."
---

## Why this matters

The OWASP Top Ten is the industry-standard awareness document for web application security. Hiring managers, compliance audits, and penetration testers all reference it. If you ship a production application without understanding these ten categories, you are gambling with user data, company reputation, and legal liability. Every year, breaches that cost millions trace back to vulnerabilities described in this list.

## Start with something familiar

A safety inspection groups common hazards so reviewers know where to look first.

For new password storage, consult the [OWASP password-storage guidance](https://cheatsheetseries.owasp.org/cheatsheets/Password_Storage_Cheat_Sheet.html): Argon2id is preferred where available; scrypt is an alternative, and bcrypt requires attention to its 72-byte input limit. Work factors are algorithm-specific, not one shared setting.

## The mental model

> The OWASP Top Ten is an awareness taxonomy of application risks, not a complete checklist or proof of security.

This lesson uses [OWASP Top 10:2025](https://top10.owasp.org/2025/) category names and identifiers. Categories organize review; a single defect may implicate several controls.

## A01 — Broken Access Control

The most common category. Users act outside their intended permissions.

```javascript
// VULNERABLE — relies on client-side route hiding
app.get("/admin/users", (req, res) => {
  // No server-side check. Anyone who knows the URL gets in.
  const users = db.query("SELECT * FROM users");
  res.json(users);
});

// FIXED — enforce authorization on every request
app.get("/admin/users", authenticate, authorize("admin"), (req, res) => {
  const users = db.query("SELECT * FROM users");
  res.json(users);
});

function authorize(role) {
  return (req, res, next) => {
    if (req.user.role !== role) {
      return res.status(403).json({ error: "Forbidden" });
    }
    next();
  };
}
```

## A04 — Cryptographic Failures

Formerly "Sensitive Data Exposure." Data in transit or at rest is not properly protected.

```javascript
// VULNERABLE — storing passwords in plaintext
async function createUser(email, password) {
  await db.query("INSERT INTO users (email, password) VALUES ($1, $2)", [
    email,
    password, // plaintext!
  ]);
}

// Legacy-compatible bcrypt example; tune cost and enforce its input byte limit
const bcrypt = require("bcrypt");

async function createUser(email, password) {
  const hash = await bcrypt.hash(password, 12);
  await db.query("INSERT INTO users (email, password_hash) VALUES ($1, $2)", [email, hash]);
}
```

## A05 — Injection

Untrusted data is sent to an interpreter as part of a command or query. SQL injection remains the classic example but command injection, LDAP injection, and NoSQL injection all belong here.

```javascript
// VULNERABLE — string concatenation in query
app.get("/users", (req, res) => {
  const name = req.query.name;
  const result = db.query(`SELECT * FROM users WHERE name = '${name}'`);
  res.json(result);
});
// Attacker sends: ?name=' OR '1'='1

// FIXED — parameterized query
app.get("/users", (req, res) => {
  const name = req.query.name;
  const result = db.query("SELECT * FROM users WHERE name = $1", [name]);
  res.json(result);
});
```

## A06 — Insecure Design

Security flaws baked into the architecture itself, not just the implementation. No amount of perfect code fixes a design that never considered abuse cases.

```javascript
// VULNERABLE — unlimited password reset attempts
app.post("/reset-password", (req, res) => {
  const code = generateSixDigitCode();
  sendEmail(req.body.email, code);
  res.json({ message: "Code sent" });
});
// Attacker brute-forces all 1,000,000 combinations

// FIXED — rate limiting + account lockout + longer codes
const rateLimit = require("express-rate-limit");

const resetLimiter = rateLimit({
  windowMs: 15 * 60 * 1000, // 15 minutes
  max: 3, // 3 attempts per window
  message: { error: "Too many attempts. Try again later." },
});

app.post("/reset-password", resetLimiter, (req, res) => {
  const code = crypto.randomBytes(16).toString("hex"); // 128-bit token
  sendEmail(req.body.email, code);
  res.json({ message: "Code sent" });
});
```

## A02 — Security Misconfiguration

Default credentials, open cloud storage buckets, verbose error messages, unnecessary features enabled.

```javascript
// VULNERABLE — stack traces in production
app.use((err, req, res, next) => {
  res.status(500).json({
    error: err.message,
    stack: err.stack, // leaks internals to attackers
  });
});

// FIXED — generic message in production, details in logs only
app.use((err, req, res, next) => {
  console.error(err); // internal logging
  res.status(500).json({
    error: process.env.NODE_ENV === "production" ? "Internal server error" : err.message,
  });
});
```

## A03 — Software Supply Chain Failures

Using libraries with known CVEs. We cover this in depth in lesson 06.

## A07 — Authentication Failures

Weak passwords, credential stuffing, missing multi-factor authentication. Covered in lesson 02.

## A08 — Software or Data Integrity Failures

Trusting unsigned updates, CI/CD pipeline compromises, insecure deserialization.

```javascript
// VULNERABLE — deserializing untrusted input
const userData = JSON.parse(req.body.data);
// If this were a language with object deserialization (Java, Python pickle),
// arbitrary code execution is possible.

// FIXED — validate structure with a schema
const { z } = require("zod");
const UserSchema = z.object({
  name: z.string().max(100),
  email: z.string().email(),
});

const parsed = UserSchema.safeParse(JSON.parse(req.body.data));
if (!parsed.success) {
  return res.status(400).json({ error: parsed.error.issues });
}
```

## A09 — Security Logging and Alerting Failures

When breaches happen and nobody notices for months, this is the category.

```javascript
// Minimum: log authentication events
function logSecurityEvent(event) {
  const entry = {
    timestamp: new Date().toISOString(),
    type: event.type, // "LOGIN_SUCCESS", "LOGIN_FAILURE", "ACCESS_DENIED"
    userId: event.userId,
    ip: event.ip,
    userAgent: event.userAgent,
  };
  // Ship to centralized logging (not just console.log)
  securityLogger.info(entry);
}
```

## A10 — Mishandling of Exceptional Conditions

Failures become security problems when unexpected states bypass controls or expose data. For example, an authorization service timeout must not default to allowing a request. A failed multi-step update must preserve its invariant through a transaction or explicit recovery, and an exception response must not expose secrets. Test timeouts, malformed inputs, resource exhaustion, and partial failures as well as successful requests.

## Related risk — Server-Side Request Forgery (SSRF)

The server fetches a URL provided by the user, allowing attackers to access internal services.

```javascript
// VULNERABLE — fetch any URL the user provides
app.get("/preview", async (req, res) => {
  const response = await fetch(req.query.url);
  const html = await response.text();
  res.send(html);
});
// Attacker sends: ?url=http://169.254.169.254/latest/meta-data/ (AWS metadata)

// FIXED — allowlist of permitted domains
const ALLOWED_HOSTS = new Set(["api.example.com", "cdn.example.com"]);

app.get("/preview", async (req, res) => {
  const url = new URL(req.query.url);
  if (!ALLOWED_HOSTS.has(url.hostname)) {
    return res.status(400).json({ error: "Domain not allowed" });
  }
  const response = await fetch(url.toString());
  const html = await response.text();
  res.send(html);
});
```

## Guided recall

<FillBlank
  question="OWASP category A05 covers ______ attacks, where untrusted data is sent to an interpreter as part of a command or query."
  answer="injection"
/>

## Check your understanding

<WrittenExercise
 instructions="Analyze the scenario and compare the relevant choices. Analyze the vulnerable login code in this lesson. Write a remediation plan that maps at least four defects to protected properties. Trace one attack from request input to its effect, then specify a regression test and the server-side controls needed before deployment."
 rubric={["Traces a concrete source-to-sink attack","Separates authentication, authorization, and input handling","Names appropriate session and password controls","Defines tests for both rejection and valid login"]}
 modelAnswer={"The concatenated SQL lets request data alter query syntax; use parameterized lookup. Plaintext password comparison exposes credentials if storage is leaked; use a vetted password-hashing library with a unique salt and current parameters. A cookie containing only a user ID is forgeable; issue an unpredictable server-side session identifier with expiry and secure cookie attributes. Generic login failures reduce account enumeration, while rate limiting bounds guessing. Test injection-like usernames as ordinary data, wrong passwords, expired sessions, and an edited cookie. Verify authorization on every protected operation; successful login alone does not grant access to all records."}
/>



<Quiz
  question="Which OWASP Top Ten category addresses the risk of users accessing resources they should not be authorized to use?"
  options={[
    "A05 — Injection",
    "A01 — Broken Access Control",
    "A07 — Authentication Failures",
    "A02 — Security Misconfiguration",
  ]}
  answer={1}
  explanation="A01 Broken Access Control covers users acting outside their intended permissions — accessing other users' data, modifying records they shouldn't, or reaching admin endpoints."
/>

<Quiz
  question="What makes A06 (Insecure Design) different from implementation bugs?"
  options={[
    "It only affects frontend code",
    "It cannot be fixed with code changes",
    "The flaw is in the architecture, not a coding mistake",
    "It only applies to legacy systems",
  ]}
  answer={2}
  explanation="Insecure Design means the system was never designed to defend against certain attacks. Perfect implementation of a flawed design is still insecure."
/>



## What you learned

- Recognize a concrete vulnerability category
- Trace input to a sensitive operation
- Choose a mitigation and verification test
