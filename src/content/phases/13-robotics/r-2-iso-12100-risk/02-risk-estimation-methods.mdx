---
title: "Risk Estimation Methods — HRN, ISO 13849 Risk Graph, and Hybrid Scoring"
description: "ISO 12100 requires risk estimation but doesn't mandate a method. HRN, the ISO 13849 risk graph, and hybrid scoring matrices each have strengths, weaknesses, and audit footprint. Pick one and document why."
phase: 13
module: "r-2-iso-12100-risk"
order: 2
estimatedMinutes: 45
difficulty: 3
prerequisites:
  - "r-2-iso-12100-risk-01"
standards:
  primaryAnchor: "ISO 12100:2010"
  cs2023: []
  swebok: []
  bloom: "apply"
  sfia: 3
  dreyfus: "competent"
vocabulary:
  - hazard-rating-number
  - risk-graph
  - severity-of-harm
  - frequency-of-exposure
  - probability-of-harm
  - possibility-of-avoidance
  - risk-matrix
  - tolerable-risk
learningOutcomes:
  - "After this lesson you can: compute a Hazard Rating Number (HRN) for a given hazard scenario using the four HRN parameters"
  - "After this lesson you can: determine a risk graph output (a, b, c, d, or e) for a given scenario using the ISO 13849-1 Annex A risk graph"
  - "After this lesson you can: state two audit objections to using HRN in a safety-integrity-rated risk assessment"
professionalContext: "HRN is popular with engineers because it produces a number. The ISO 13849 risk graph is popular because it directly outputs a required performance level. Knowing when to use each — and knowing that mixing them in one assessment is a finding — is the decision a lead safety engineer makes before the first line of the hazard register."
thresholdConcept: false
---

## Why the method choice matters more than the numbers

ISO 12100:2010 Step 3 (risk estimation) requires you to estimate the risk for each identified hazard. The standard specifies the parameters to consider but does not mandate a specific scoring method. This gives integrators flexibility but creates a decision: which method?

The method choice is not arbitrary. Different methods produce different outputs that integrate differently with downstream analyses. Using HRN when your risk assessment must feed a PL determination per ISO 13849 is architecturally awkward. Using the ISO 13849 risk graph when you need a free-form severity ladder is underpowered. The method is an architectural decision, and it should be documented explicitly with justification.

## Method 1 — Hazard Rating Number (HRN)

HRN is a numerical scoring method that multiplies four independent factors:

```
HRN = LO × FE × DPH × NP
```

Where:

- **LO (Likelihood of Occurrence)**: probability that the hazardous event occurs given exposure. Scale: 0.033 (almost impossible) to 15 (certain).
- **FE (Frequency of Exposure)**: how often a person is in the hazard zone. Scale: 0.5 (rarely, once per year) to 5 (constantly).
- **DPH (Degree of Possible Harm)**: severity of injury. Scale: 0.1 (slight injury) to 15 (multiple deaths).
- **NP (Number of Persons at Risk)**: 1–12 (or more for group hazards).

HRN bands map to action levels:

| HRN      | Risk Level  | Required Action                |
| -------- | ----------- | ------------------------------ |
| 0–1      | Acceptable  | No action required             |
| 1–5      | Very low    | Review possible improvements   |
| 5–10     | Low         | Review and improve if possible |
| 10–50    | Significant | Improvement necessary          |
| 50–500   | High        | Urgent improvement needed      |
| 500–1000 | Very high   | Immediate action required      |
| >1000    | Extreme     | Stop the operation             |

### HRN worked example

**Hazard**: robot wrist motion during setup causes a pinch between the wrist joint and the fixture while the operator manually adjusts a part.

- LO = 1 (possible — operators are present during setup and pinch scenarios have occurred in similar cells)
- FE = 3 (frequently — operators reach into the cell 10–15 times per shift for adjustments)
- DPH = 4 (serious injury — a finger amputation is plausible for a wrist pinch at maximum torque)
- NP = 1 (one operator at a time)

```
HRN = 1 × 3 × 4 × 1 = 12 → Significant — improvement necessary
```

This hazard requires a risk-reduction measure. The HRN will be recomputed after controls are applied (Clause 6) to confirm residual risk is tolerable.

### When NOT to use HRN

HRN outputs a dimensionless number on a proprietary scale. It does NOT output a performance level (PL) or safety integrity level (SIL). If your risk assessment must determine the required PL for a safety function, you cannot drive the determination from HRN alone — HRN was not designed for that purpose and its factor scales don't map to the ISO 13849 risk graph's severity/frequency/avoidance framework.

Auditors will ask: "How did you determine the required PL for this safety function?" If the answer is "We computed HRN and it came out in the 'significant' band, so we chose PL d," that's a finding. The PL determination requires the ISO 13849 risk graph, not HRN.

## Method 2 — ISO 13849-1 Annex A Risk Graph

The ISO 13849-1:2023 risk graph is the standard method for determining the required Performance Level (PL_r) of a safety function. It uses four parameters, each with discrete categories:

```
S (Severity of injury)
  S1 — Slight (normally reversible)
  S2 — Serious (normally irreversible), including death

F (Frequency and/or duration of exposure)
  F1 — Seldom to less often and/or short duration
  F2 — Frequent to continuous and/or long duration

P (Possibility of avoiding hazard or limiting harm)
  P1 — Possible under specific conditions
  P2 — Scarcely possible

O (Probability of occurrence of the hazardous event)
  O1 — Very low
  O2 — Low
  O3 — Relatively high
```

ISO 13849-1:2023 updated the graph to add the O parameter (Probability of Occurrence) to the original three-parameter S/F/P graph from the 2006 edition. The 2023 output is not just PL_a through PL_e but a more granular risk determination.

### Risk graph worked example

Same hazard: pinch between robot wrist and fixture during setup.

- S = S2 (finger amputation is normally irreversible)
- F = F2 (frequent — operators reach in 10–15 times per shift)
- P = P2 (scarcely possible to avoid — the robot wrist can close before the operator can react)
- O = O2 (low probability — requires the robot to be moving during the specific operator action, which should not occur if the SMS mode is active)

Reading the 2023 graph for S2/F2/P2/O2: **PL d** is the required performance level.

This output is directly usable: the safety function (monitored-stop) preventing the robot from moving during setup must be designed to PL d per ISO 13849.

### When to use the risk graph

Use the ISO 13849 risk graph when the output of your risk assessment must be a PL requirement for a safety function. This is the case whenever you are applying ISO 10218-2 (robot system integration), which references ISO 13849 for the safety-function design.

## Method 3 — Simple Risk Matrix (5×5 or 3×3)

A risk matrix maps severity (columns) against probability (rows) to a risk level. It's the least rigorous of the three methods but the most visually accessible.

```
            Negligible  Minor  Serious  Severe  Critical
Improbable     L         L       L       M        M
Remote         L         L       M       M        H
Occasional     L         M       M       H        H
Frequent       M         M       H       H        VH
Continuous     M         H       H       VH       VH
```

(L=Low, M=Medium, H=High, VH=Very High)

A matrix is appropriate when:

- The risk assessment is high-level (screening, not final design)
- The output is risk level for management reporting, not a PL determination
- The team has no ISO 13849 analysis requirement

A matrix is NOT appropriate when the output must be a PL or SIL claim.

## Mixing methods — the finding pattern

The most common audit finding in risk estimation is inconsistent method application:

- Section 1 of the assessment uses HRN
- Section 2 uses the risk graph
- Section 3 uses a 3×3 matrix
- The summary table maps all three to a common "High / Medium / Low" scale without documenting the translation logic

An auditor cannot audit a mixed-method assessment without first reconstructing the translation logic, and often the translation was not principled. The finding: "risk estimation method is inconsistent; risk levels cannot be compared across hazards; re-assess with a single documented method."

## Check your understanding

<SandboxExercise
  title="Method Selection Decision"
  description="You are the lead safety engineer for a new robot cell. The customer specifies: (a) the cell must have a documented risk assessment per ISO 12100, and (b) all safety functions must be rated to a minimum of PL c. You need to select a risk estimation method."
  task="State which method you select, explain why it's the right choice for this customer requirement, and name one condition under which you would choose differently."
  acceptanceCriteria={[
    "Selects the ISO 13849-1 risk graph",
    "Justifies the selection: the risk graph outputs PL_r directly, which is necessary to drive the safety-function design that the customer's PL c minimum requirement depends on",
    "Names a valid alternative condition — e.g., if the assessment is a high-level screening exercise before safety functions are designed, a risk matrix would be appropriate for the initial pass",
  ]}
/>

<Quiz
  questions={[
    {
      question:
        "An HRN calculation for a robot arm impact hazard produces HRN = 75 (High — urgent improvement needed). The integrator wants to document the required Performance Level for the protective-stop safety function. Can they use the HRN result to determine the PL requirement directly?",
      options: [
        "Yes — HRN 75 maps to PL d by the HRN-to-PL correspondence table",
        "No — HRN is not designed to output a Performance Level; the PL determination requires the ISO 13849-1 risk graph, which uses different parameter categories (S, F, P, O) that don't map from HRN factors",
        "Yes — any risk level above 'Low' requires PL d by ISO 12100's default assignment",
        "No — HRN cannot be used at all in safety-certified machinery assessments",
      ],
      correct: 1,
      explanation:
        "HRN produces a dimensionless score on a proprietary scale. It was not designed to output Performance Levels. The ISO 13849-1 risk graph uses discrete parameters (Severity S1/S2, Frequency F1/F2, Possibility P1/P2, Occurrence O1-O3) whose output is directly PL_r for the safety function. Using HRN to determine PL would require a documented, defensible mapping between the two methods — which doesn't exist in any normative standard.",
    },
  ]}
/>

## What's next

Lesson R2-3 covers the residual risk document — the output of Clause 6 risk reduction. You'll see how to format a residual risk register so it's auditable, traceable from the original hazard, and complete even when some risks cannot be reduced to zero.
