---
title: "What Formal Methods Are For"
description: "Learn how mathematical specification and verification expose specification violations beyond sampled tests — and when to use them in real systems."
phase: 7
module: "7-5"
order: 1
estimatedMinutes: 15
difficulty: 5
standards: {"cs2023":["SE-2","SE-4"],"swebok":["Software Quality","Software Testing"],"bloom":"understand","sfia":4,"dreyfus":"competent"}
vocabulary: ["formal-specification","model-checking","state-space","invariant","temporal-logic","refinement","liveness","safety-property"]
learningOutcomes: ["Explain the difference between safety and liveness properties and the limits of sampled tests","Distinguish model checking from theorem proving and describe when each is appropriate","Explain how a model can expose a concurrency failure without proving an implementation correct","Write a plain-language invariant for a given system and translate it into a logical predicate"]
prerequisites: ["3/3-1/01","7/7-2/01"]
thresholdConcept: false
---

## Why this matters

Testing observes selected executions. Formal verification checks precisely stated properties under explicit assumptions: a finite model checker explores reachable modeled states, while a proof establishes a theorem from its premises. Neither automatically proves that production software matches the model. This distinction matters especially for concurrency, where a small change in event order can violate an invariant.

Understanding formal methods makes you a better engineer even when you are not writing specifications yourself. The discipline of stating what a system _must never do_ and what it _must eventually do_ is the same discipline that produces clean architecture, useful code reviews, and bug reports that can actually be reproduced. Formal methods are sharpened thinking applied to software.

## Start with something familiar

A scale model can expose an impossible doorway before a full building is constructed.

## The mental model

> Formal methods express a system model and properties precisely enough to analyze. The conclusions are limited by the model, its assumptions, and the checks actually performed.

## The gap that testing leaves open

Imagine you are testing a bank transfer function. You try sending $0, $1, $1,000,000, and a negative amount. All pass. Six months later, two concurrent transfers drain the same account into the red. You never tested two calls happening at exactly the same millisecond because your test suite is single-threaded.

The missing concurrent test could expose this bug. The broader difficulty is coverage: ten nodes with twenty local states have up to 20¹⁰ = 10,240,000,000,000 combinations before accounting for message queues. Not every combination is reachable. At one million combinations per second, enumerating that many takes about 119 days, illustrating why abstraction and efficient exploration matter.

> **Formal methods are just a way of writing down what you already believe to be true — then having a computer check whether your belief is consistent.**

The approach: instead of running the real system, you build a _model_ — a precise, abstract description of the system's possible behaviors. Then you state the properties you care about, and a tool exhaustively searches the model's state space to find any execution that violates them. If a violation exists, the tool shows you the exact sequence of steps that leads to it.

## Two kinds of properties

Safety and liveness are two useful property classes; a requirement can combine both:

<VocabTooltip slug="safety-property">**Safety properties**</VocabTooltip> say "this bad thing never
happens." Examples: two processes never hold the mutex simultaneously; the account balance never
goes negative; a packet is never delivered more than once. Safety violations can be demonstrated
with a finite execution trace — there is a specific sequence of steps that reaches the bad state.

<VocabTooltip slug="liveness">**Liveness properties**</VocabTooltip> say "this good thing eventually
happens." Examples: every request eventually gets a response; a process waiting to acquire the lock
will eventually acquire it; a message sent will eventually be delivered. Liveness violations require
infinite traces — you must show that the system can loop forever without ever reaching the desired
state.

Testing can sometimes catch safety violations if you trigger the right execution. A timeout can reveal a missed deadline, and inspection can expose a deadlock. A finite observation alone, however, cannot establish unbounded eventual progress: "not yet" and "never" are different claims.

```
Safety:  "the system NEVER reaches a bad state"
         → can be falsified by one finite trace
         → testing might catch this if you're lucky

Liveness: "the system ALWAYS eventually reaches a good state"
          → requires ruling out infinite non-progress
          → testing cannot verify this — you'd need to run forever
```

## Model checking vs. theorem proving

There are two main flavors of formal verification:

<VocabTooltip slug="model-checking">**Model checking**</VocabTooltip> is automated. You describe the
system as a finite (or bounded) state machine, state the properties you want to verify in
<VocabTooltip slug="temporal-logic">temporal logic</VocabTooltip>, and the tool enumerates all
reachable states and checks each one. Tools: TLA+/TLC, Alloy, Spin. The key advantage is that when a
property is violated, the tool gives you a concrete counterexample — the exact sequence of states
that leads to the bug. The key limitation: state-space explosion. Real systems have enormous state
spaces; model checkers only work if you abstract aggressively.

**Theorem proving** uses a proof assistant such as Coq, Isabelle, or Lean to check a proof in a formal logic. It can establish properties of infinite families of states when an appropriate proof exists. Automation and human effort depend on the theorem and representation.

For an initial model-checking exercise, use two workers and one shared balance. Model each worker reading the balance, deciding whether funds suffice, and writing a withdrawal. Both can approve against the same old balance. Checking conservation across the balance and completed withdrawals exposes the lost update. An atomic check-and-withdraw transition removes that modeled interleaving; implementation testing must still verify the real transaction behavior.

## What formal methods are not

Formal methods verify the _model_, not the code. A model-level result applies to the implementation only to the extent that their relationship is justified. Some verification projects prove properties of implementation code as well; a design model alone does not supply that proof. This is why formal methods are most valuable during design, when you are reasoning about protocols and invariants — not as a replacement for code review or testing, but as a complement that catches structural flaws before they are baked into thousands of lines of implementation.

Formal methods also do not verify that you specified the _right_ properties. If you forget to state that the account balance must never go negative, the tool will not check it. The quality of verification is bounded by the quality of your specifications.

## Invariants — the foundation of specification

An <VocabTooltip slug="invariant">invariant</VocabTooltip> is a condition that must be true in every reachable state of your system. Writing invariants is the first skill of formal specification.

```
System: A bounded queue with capacity N

Plain language invariants:
  1. The queue never contains more than N items
  2. Items are removed in the same order they were inserted
  3. Dequeue never returns an item that was not enqueued

In predicate logic:
  1. ∀ state s: len(s.queue) ≤ N
  2. dequeued_history is a prefix of enqueued_history
  3. enqueued_history = dequeued_history concatenated with s.queue

Assumptions: successful operations only; FIFO order is the model’s
atomic enqueue order; histories record unique occurrence IDs so repeated
values remain distinguishable.
```

The act of writing these down precisely often reveals ambiguity you didn't know existed. Does "order" mean insertion order or priority? Does capacity refer to bytes or items? Invariant-writing forces precision that hand-waving design discussions never achieve.

## Practice

<FillBlank
  prompt="A ___ property states that something bad never happens and can be falsified by a finite execution trace, while a ___ property states that something good eventually happens and requires reasoning about infinite executions."
  answers={["safety", "liveness"]}
  hints={["Think about the two fundamental categories of system properties that formal methods can verify."]}
/>

## Check your understanding

<Quiz questions={[
    {
      question:
        "A distributed lock manager passes 10,000 tests. Which conclusion is justified?",
      options: [
        "Those executions passed; an untested interleaving may still violate a property",
        "Every possible deadlock has been ruled out",
        "All safety properties hold for every input",
        "The implementation necessarily matches its formal model",
      ],
      correct: 0,
      explanation:
        "Passing tests is evidence about the executions tested. Both ordinary input bugs and concurrency bugs can remain outside that coverage; a test count alone supplies no exhaustive guarantee.",
    },
    {
      question:
        "You are designing a payment processing protocol and want to prove that no money is ever created or destroyed by the system (conservation of funds). This is best expressed as:",
      options: [
        "A liveness property — funds must eventually reach the destination",
        "A safety property — the sum of all balances never changes",
        "A refinement — the implementation matches a simpler model",
        "A temporal logic formula — funds flow in the correct direction over time",
      ],
      correct: 1,
      explanation:
        "Conservation of funds is a safety property: 'bad thing (balance sum changing) never happens.' It can be checked by verifying an invariant — that sum(all_balances) is constant — holds in every reachable state.",
    },
    {
      question:
        "Model checking finds a counterexample to your safety property. What does the counterexample represent?",
      options: [
        "A random test case the tool generated",
        "An approximation of likely failure modes",
        "A concrete sequence of states leading to a property violation",
        "A proof that the property is undecidable",
      ],
      correct: 2,
      explanation:
        "Model checkers perform exhaustive state-space search and, when they find a violation, produce a concrete execution trace — the exact sequence of steps from the initial state to the violating state. This trace is directly usable for debugging.",
    },
  ]} />

## What you learned

- A completed finite model check establishes the checked properties for reachable states within its model and bounds; an implementation still needs separate evidence
- Safety properties ("bad thing never happens") can be falsified by finite traces; liveness properties ("good thing eventually happens") require ruling out infinite non-progress
- Model checking (TLA+, Alloy, Spin) is automated and produces concrete counterexamples; theorem proving (Coq, Lean) handles infinite-state systems but requires proof expertise
- Writing invariants forces the precision that distinguishes a verified design from a hand-waved one
- Formal methods verify the model — they complement, not replace, testing and code review
