---
title: "Logical Patterns"
description: "Short-circuit evaluation, default values, and the patterns that show up in real codebases."
phase: 1
module: "1-2-control-flow"
order: 6
estimatedMinutes: 10
difficulty: 3
standards: {"cs2023":["SDF-1","PL-2"],"swebok":["Software Design"],"bloom":"apply","sfia":3,"dreyfus":"advanced-beginner"}
vocabulary: ["conditional"]
learningOutcomes: ["Write a function greet(name, greeting) that defaults greeting to 'Hello' using ?? and returns '{greeting}, {name}!'.","Explain a reasoned answer to: What does this print? console.log(0 || 'fallback');","Explain a reasoned answer to: What does this print? console.log(0 ?? 'fallback');"]
prerequisites: ["1/1-2/12"]
thresholdConcept: false
---

## Why this matters

Professional code is full of `||`, `&&`, `??`, and ternary patterns that look cryptic until you see the logic. This lesson breaks down the four most common patterns so you can read and write them confidently.

## Start with something familiar

Imagine choosing a substitute only when a form field is missing, compared with replacing any answer that looks empty. A written zero may be a valid answer.

## The mental model

> Short-circuit operators decide whether to evaluate their right operand. Nullish defaults distinguish missing values from other falsy values such as zero.

## Pattern 1: Default value with ||

```javascript
const name = userName || "Anonymous";
```

`||` returns the first **truthy** operand, or the last one. If `userName` is `""`, `null`, `undefined`, `0`, or `false`, name becomes `"Anonymous"`.

Problem: `0` and `""` are legitimate values that `||` treats as falsy.

## Pattern 2: Default value with ?? (better)

```javascript
const count = userCount ?? 10;
```

`??` returns the left side unless it is `null` or `undefined`. `0` and `""` pass through. Use `??` when zero or empty string are valid values.

## Pattern 3: Conditional execution with &&

```javascript
isAdmin && showAdminPanel();
```

`&&` returns the first **falsy** operand, or the last one. If `isAdmin` is falsy, `showAdminPanel()` never runs. This is the short-circuit pattern — the right side is only evaluated if the left side is truthy.

In React JSX you see this constantly:

```javascript
{
  isLoggedIn && <Dashboard />;
}
```

## Pattern 4: Ternary for inline values

```javascript
const label = count === 1 ? "item" : "items";
```

Clean for a single choice. Do not chain ternaries — they become unreadable.

```javascript
// Don't do this:
const color = status === "ok" ? "green" : status === "warn" ? "yellow" : "red";
// Do this instead:
function statusColor(status) {
  if (status === "ok") return "green";
  if (status === "warn") return "yellow";
  return "red";
}
```

## Guided recall

<FillBlank
  prompt="`||` returns the first ___ value. `??` falls through only on ___ or ___."
  answers={["truthy", "null", "undefined"]}
  hints={["The opposite of falsy.", "The deliberate nothing.", "The accidental nothing."]}
/>

## Check your understanding

<SandboxExercise
  language="javascript"
  instructions="Write a function greet(name, greeting) that defaults greeting to 'Hello' using ?? and returns '{greeting}, {name}!'."
  initialCode={`function greet(name, greeting) {
  // your code here
}

console.log(greet("Ada", "Hi")); // "Hi, Ada!"
console.log(greet("Ada")); // "Hello, Ada!"
console.log(greet("Ada", "")); // ", Ada!" — empty string is intentional`}
  solution={`function greet(name, greeting) {
const g = greeting ?? "Hello";
return \`\${g}, \${name}!\`;
}

console.log(greet("Ada", "Hi"));
console.log(greet("Ada"));
console.log(greet("Ada", ""));`}
testCases={["greet defaults correctly"]}
/>

<Quiz
  questions={[
    {
      type: "code-output",
      question: "What does this print?\n```js\nconsole.log(0 || 'fallback');\n```",
      options: ["0", "'fallback'", "false", "null"],
      correct: 1,
      explanation: "0 is falsy, so || skips it and returns 'fallback'.",
    },
    {
      type: "code-output",
      question: "What does this print?\n```js\nconsole.log(0 ?? 'fallback');\n```",
      options: ["0", "'fallback'", "null", "undefined"],
      correct: 0,
      explanation: "?? only falls through on null/undefined. 0 is not null or undefined.",
    },
  ]}
/>



## What you learned

- `||` defaults on any falsy value; `??` defaults only on null/undefined. Prefer `??`.
- `&&` short-circuits: the right side only runs if the left side is truthy.
- Ternary is great for one-line choices; don't chain them.
