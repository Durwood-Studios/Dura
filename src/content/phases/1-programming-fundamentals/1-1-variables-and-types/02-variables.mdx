---
title: "Variables"
description: "Names for values. The first thing programs do is remember things."
phase: 1
module: "1-1-variables-and-types"
order: 2
estimatedMinutes: 10
difficulty: 1
standards: {"cs2023":["SDF-1","PL-1"],"swebok":["Software Design"],"bloom":"apply","sfia":2,"dreyfus":"novice"}
vocabulary: ["variable"]
prerequisites: ["1/1-1/01"]
learningOutcomes: ["Declare named values and display a sentence that uses them","Trace reassignment by evaluating the right-hand expression first","Explain why reading an undeclared name throws an error"]
thresholdConcept: true
---

## Why this matters

A program that cannot remember anything is not much of a program. **Variables** are how a program remembers: you give a value a name, and later you ask for that name and get the value back. That is the whole idea. Every program you will ever write is built on it.

## Start with something familiar

Imagine a coat-check ticket attached to a coat. You can use its label to refer to the coat without describing it again.

## The mental model

> A variable is a binding between a name and a value. Assignment evaluates the right side before updating the binding on the left.

## The basic shape

```javascript
const greeting = "Hello, world.";
console.log(greeting);
```

Line 1 creates a variable named `greeting` and stores the string `"Hello, world."` in it. Line 2 looks the name up and prints what is there.

The `=` is not a math equals sign — it is **assignment**. It means "take the value on the right and put it in the name on the left."

## Declare before you use

In JavaScript you have to declare a variable before you can use it. Declaration is the part with `const`, `let`, or `var`.

```javascript
const name = "Dustin";
let age = 37;
```

Try to use an undeclared name and JavaScript throws an error.

```javascript
console.log(undeclaredName); // Expected ReferenceError: the name was never declared
```

Reading an undeclared name throws in strict and non-strict JavaScript. Strict mode additionally rejects assigning to an undeclared name.

## Reassignment

A `let` variable can be given a new value later.

```javascript
let score = 0;
score = score + 10;
console.log(score); // 10
```

Read that second line carefully: "take the current value of `score`, add 10, store the result back in `score`." The right side is evaluated first, then the assignment happens. This is how counters, totals, and accumulators work.

A `const` variable cannot be reassigned. More on that next lesson.

## Naming rules

Names in JavaScript:

- Start with a letter, `_`, or `$`.
- Contain letters, digits, `_`, `$`.
- Are case-sensitive (`age` and `Age` are different).
- Cannot be reserved words (`if`, `function`, `class`, etc.).

Naming convention: **camelCase** for variables and functions. `userName`, `totalPrice`, `isActive`. Not `user_name` or `UserName`.

A good name describes **what** the value means, not how it is stored. `count` is better than `i`. `totalPrice` is better than `x`.

## Guided practice

Complete the recall prompt before attempting the programming task.

<FillBlank
  prompt="The = operator is called ___. The standard JavaScript naming convention for variables is ___."
  answers={["assignment", "camelCase"]}
  hints={["What = does.", "firstWordLowercase, otherWordsCapitalized."]}
/>



> **What changes for you after this**
> Before this lesson a variable name may have looked like part of the value. Afterward, you can trace a name being rebound while the expression on the right is evaluated first.

## Check your understanding

<SandboxExercise
  language="javascript"
  instructions="Create a variable for your name and one for your current year, then log a sentence that uses both."
  initialCode={`// Declare two variables and log a sentence using them.

`}
  solution={`const name = "Dustin";
const year = 2026;
console.log("My name is " + name + " and the year is " + year + ".");`}
testCases={["logs a sentence with name and year"]}
/>

Explain the following results after completing the programming task.

<Quiz
  questions={[
    {
      type: "multiple-choice",
      question: "What does the `=` operator do in JavaScript?",
      options: [
        "Checks if two values are equal",
        "Assigns the right-hand value to the left-hand name",
        "Multiplies two values",
        "Declares a new variable",
      ],
      correct: 1,
      explanation: "= is assignment. Equality is == or ===.",
    },
    {
      type: "multiple-choice",
      question: "What happens if you try to read an undeclared variable?",
      options: [
        "It returns undefined",
        "It returns null",
        "It throws a ReferenceError",
        "It creates the variable silently",
      ],
      correct: 2,
      explanation: "Reading an undeclared name in this expression throws a ReferenceError in both strict and non-strict code.",
    },
  ]}
/>



## What you learned

- Declare named values and display a sentence that uses them.
- Trace reassignment by evaluating the right-hand expression first.
- Explain why reading an undeclared name throws an error.
