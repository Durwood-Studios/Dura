---
title: "Balanced Trees"
description: "Why BSTs can degenerate, what balance means, and how AVL and red-black trees maintain O(log n) guarantees."
phase: 3
module: "3-4-trees-and-graphs"
order: 4
estimatedMinutes: 15
difficulty: 3
standards: {"cs2023":["AL-3","DS-2"],"swebok":["Software Construction"],"bloom":"understand","sfia":3,"dreyfus":"competent"}
vocabulary: ["avl-tree","red-black-tree","rotation","balance-factor","self-balancing"]
learningOutcomes: ["Explain a reasoned answer to: What is the balance factor of a node?","Explain a reasoned answer to: Red-black trees provide a stricter height guarantee than AVL trees.","Explain a reasoned answer to: JavaScript's built-in Map and Set are backed by which data structure in V8?"]
prerequisites: ["3/3-4/03"]
thresholdConcept: false
---

## Why this matters

A BST's O(log n) promise depends entirely on the tree staying reasonably short. As you saw in the previous lesson, inserting sorted data creates a linked list — O(n) operations instead of O(log n). Real-world data isn't random, so degeneration is a real risk, not a theoretical edge case. Balanced trees solve this by automatically restructuring themselves after insertions and deletions to keep the height bounded.

## Start with something familiar

A branching directory becomes inefficient if every new entry extends the same long hallway.

## The mental model

> Balancing limits tree height while preserving search order. Rotations change local shape without changing the inorder sequence.

## What "balanced" means

A tree is **height-balanced** if no path from root to leaf is dramatically longer than any other. More precisely, balance means the height is guaranteed to be O(log n) regardless of insertion order.

The key insight: in a binary tree with n nodes, the minimum possible height is ⌊log₂(n)⌋ (perfect tree). We want self-balancing trees to stay close to this minimum — specifically, within a constant factor.

The **balance factor** of a node is defined as:

```
balance_factor(node) = height(node.left) - height(node.right)
```

- Balance factor of 0: subtrees are equally tall.
- Balance factor of +1: left subtree is one level taller.
- Balance factor of -1: right subtree is one level taller.
- |balance factor| > 1: the node is **unbalanced** and needs fixing.

## AVL trees: strict balance

An **AVL tree** (Adelson-Velsky and Landis, 1962) maintains the invariant that every node's balance factor is -1, 0, or +1. After every insert or delete, the tree checks balance factors from the modified node back up to the root and performs **rotations** wherever needed.

### Rotations

A rotation is a local restructuring that preserves the BST property while changing the tree's shape. There are four cases:

**Right rotation** (fix a left-heavy node):

```
    z              y
   / \            / \
  y   T4   →    x   z
 / \                / \
x   T3            T3   T4
```

Node z is left-heavy (balance factor +2). We promote y to z's position. z becomes y's right child. y's former right subtree T3 becomes z's new left child. The BST property is maintained because T3 has values between y and z.

**Left rotation** (fix a right-heavy node): mirror image of right rotation.

**Left-Right rotation**: node is left-heavy, but the left child is right-heavy. First rotate the left child left, then rotate the current node right.

**Right-Left rotation**: node is right-heavy, but the right child is left-heavy. Mirror image.

```javascript
function rotateRight(z) {
  const y = z.left;
  const T3 = y.right;

  // Perform rotation
  y.right = z;
  z.left = T3;

  // Update heights (z first, since it's now lower)
  z.height = 1 + Math.max(height(z.left), height(z.right));
  y.height = 1 + Math.max(height(y.left), height(y.right));

  return y; // y is the new root of this subtree
}

function rotateLeft(z) {
  const y = z.right;
  const T2 = y.left;

  y.left = z;
  z.right = T2;

  z.height = 1 + Math.max(height(z.left), height(z.right));
  y.height = 1 + Math.max(height(y.left), height(y.right));

  return y;
}
```

An AVL tree guarantees height ≤ 1.44 log₂(n) — at most 44% taller than the theoretical minimum. In practice this is excellent.

## Red-black trees: looser balance, faster updates

A **red-black tree** is a BST where every node is colored red or black. It maintains five properties:

1. Every node is red or black.
2. The root is black.
3. Every null pointer ("nil leaf") is black.
4. A red node cannot have a red child (no two consecutive red nodes).
5. Every path from any node to its null descendants has the same number of black nodes (**black-height**).

These five rules together guarantee that the tree's height is at most **2 log₂(n+1)** — at most twice the minimum. This is a slightly looser guarantee than AVL, but red-black trees require fewer rotations on average during insert and delete, making them faster for update-heavy workloads.

**Where you already use red-black trees**: JavaScript's `Map` and `Set` in V8 (Chrome's JS engine) use red-black trees internally. When you call `map.get(key)`, that's an O(log n) red-black tree lookup. Same for `set.has(value)`.

## Treaps and skip lists

Two other approaches worth knowing by name:

**Treap**: a BST where each node also has a random priority, and the tree is structured as both a BST (by key) and a max-heap (by priority). Random priorities make degeneration statistically extremely unlikely — expected height is O(log n).

**Skip list**: not a tree at all, but a layered linked list that achieves O(log n) expected operations by maintaining multiple "express lanes" of pointers. Used in Redis and some database indexes.

## The practical takeaway

You will almost never implement an AVL or red-black tree from scratch in production — the language runtime or standard library provides them. What matters is understanding:

- Why plain BSTs aren't safe for production data
- That self-balancing structures exist and what they guarantee
- That your language's built-in Map, Set, and sorted containers use these structures

## Try it

<SandboxExercise
 language="javascript"
 instructions={"Complete rotateLeft as the mirror of the provided right rotation. Reconnect the transferred subtree, update the old root's height before the new root's height, and return the new root. Explain why inorder value order survives the rotation."}
 initialCode={"class TreeNode {\nconstructor(val) {\n  this.val = val;\n  this.left = null;\n  this.right = null;\n  this.height = 1; // AVL uses height to track balance\n}\n}\n\nfunction height(node) {\nreturn node === null ? 0 : node.height;\n}\n\nfunction getBalance(node) {\nif (node === null) return 0;\nreturn height(node.left) - height(node.right);\n}\n\nfunction rotateRight(z) {\nconst y = z.left;\nconst T3 = y.right;\n\n// Rotate\ny.right = z;\nz.left = T3;\n\n// Update heights — z is now lower, update it first\nz.height = 1 + Math.max(height(z.left), height(z.right));\ny.height = 1 + Math.max(height(y.left), height(y.right));\n\nreturn y; // new subtree root\n}\n\nfunction rotateLeft(z) {\n// TODO: implement as mirror of rotateRight\n// y = z.right, T2 = y.left\n// y.left = z, z.right = T2\n// update heights, return y\n}\n\n// Build a right-skewed BST by inserting [1, 2, 3] — degenerate\nfunction buildDegenerate() {\nlet root = new TreeNode(1);\nroot.right = new TreeNode(2);\nroot.right.right = new TreeNode(3);\n// Update heights\nroot.right.right.height = 1;\nroot.right.height = 2;\nroot.height = 3;\nreturn root;\n}\n\nconst degen = buildDegenerate();\nconsole.log(\"Before rotation:\");\nconsole.log(\"Root:\", degen.val, \"balance:\", getBalance(degen)); // -2 (right-heavy, unbalanced)\n\n// Fix with a left rotation on root\nconst balanced = rotateLeft(degen);\nconsole.log(\"After left rotation:\");\nconsole.log(\"New root:\", balanced.val);\nconsole.log(\"Balance of new root:\", getBalance(balanced)); // should be 0 or ±1\nconsole.log(\"Left child:\", balanced.left ? balanced.left.val : null); // 1\nconsole.log(\"Right child:\", balanced.right ? balanced.right.val : null); // 3\n"}
 solution={"class TreeNode {\nconstructor(val) {\n  this.val = val;\n  this.left = null;\n  this.right = null;\n  this.height = 1; // AVL uses height to track balance\n}\n}\n\nfunction height(node) {\nreturn node === null ? 0 : node.height;\n}\n\nfunction getBalance(node) {\nif (node === null) return 0;\nreturn height(node.left) - height(node.right);\n}\n\nfunction rotateRight(z) {\nconst y = z.left;\nconst T3 = y.right;\n\n// Rotate\ny.right = z;\nz.left = T3;\n\n// Update heights — z is now lower, update it first\nz.height = 1 + Math.max(height(z.left), height(z.right));\ny.height = 1 + Math.max(height(y.left), height(y.right));\n\nreturn y; // new subtree root\n}\n\nfunction rotateLeft(z) {\nconst right = z.right; if (right === null) throw new Error(\"Left rotation requires a right child\"); z.right = right.left; right.left = z; z.height = 1 + Math.max(height(z.left), height(z.right)); right.height = 1 + Math.max(height(right.left), height(right.right)); return right;\n}\n\n// Build a right-skewed BST by inserting [1, 2, 3] — degenerate\nfunction buildDegenerate() {\nlet root = new TreeNode(1);\nroot.right = new TreeNode(2);\nroot.right.right = new TreeNode(3);\n// Update heights\nroot.right.right.height = 1;\nroot.right.height = 2;\nroot.height = 3;\nreturn root;\n}\n\nconst degen = buildDegenerate();\nconsole.log(\"Before rotation:\");\nconsole.log(\"Root:\", degen.val, \"balance:\", getBalance(degen)); // -2 (right-heavy, unbalanced)\n\n// Fix with a left rotation on root\nconst balanced = rotateLeft(degen);\nconsole.log(\"After left rotation:\");\nconsole.log(\"New root:\", balanced.val);\nconsole.log(\"Balance of new root:\", getBalance(balanced)); // should be 0 or ±1\nconsole.log(\"Left child:\", balanced.left ? balanced.left.val : null); // 1\nconsole.log(\"Right child:\", balanced.right ? balanced.right.val : null); // 3\n"}
 testCases={["The implementation preserves the stated invariant and produces the supplied example results"]}
/>

## Check your understanding

<Quiz
  question="What is the balance factor of a node?"
  options={[
    "The ratio of left to right children",
    "height(left) - height(right)",
    "The depth of the node in the tree",
    "The number of rotations applied",
  ]}
  answer={1}
  explanation="The balance factor is defined as height(left subtree) - height(right subtree). AVL trees maintain the invariant that every node's balance factor is -1, 0, or +1. A balance factor of +2 or -2 means a rotation is needed."
/>

<Quiz
  question="Red-black trees provide a stricter height guarantee than AVL trees."
  options={["True", "False"]}
  answer={1}
  explanation="False. AVL trees maintain the stricter guarantee (height ≤ 1.44 log n). Red-black trees allow height up to 2 log n — a looser bound. The trade-off is that red-black trees require fewer rotations on average, making them faster for insert and delete."
/>

<Quiz
  question="JavaScript's built-in Map and Set are backed by which data structure in V8?"
  options={["Hash table only", "AVL tree", "Red-black tree", "Skip list"]}
  answer={2}
  explanation="V8 (Chrome/Node.js) implements Map and Set using red-black trees, which provide O(log n) worst-case performance. Some smaller maps may fall back to hash tables as an optimization, but the guaranteed structure is a red-black tree."
/>

<FillBlank
  question="An AVL tree performs a ___ to restore balance when a subtree becomes too tall on one side."
  answer="rotation"
/>

## What you learned

- Unbalanced BSTs degrade to O(n) — self-balancing trees prevent this
- **Balance factor** = height(left) - height(right); AVL trees keep it in {-1, 0, +1}
- **Rotations** restructure the tree locally while preserving the BST property
- **AVL trees**: stricter balance (≤ 1.44 log n height), more rotations
- **Red-black trees**: looser balance (≤ 2 log n height), fewer rotations — used in JS Map and Set
