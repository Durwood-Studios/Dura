---
title: "Tree Traversal"
description: "Inorder, preorder, postorder, and level-order — four ways to visit every node, each with different uses."
phase: 3
module: "3-4-trees-and-graphs"
order: 2
estimatedMinutes: 15
difficulty: 1
standards: {"cs2023":["AL-3","DS-2"],"swebok":["Software Construction"],"bloom":"apply","sfia":3,"dreyfus":"advanced-beginner"}
vocabulary: ["inorder","preorder","postorder","level-order","depth-first","breadth-first"]
learningOutcomes: ["Implement inorder, preorder, postorder, and levelOrder for the supplied binary tree. Return arrays of values, and return an empty array for null. Preserve each traversal's stated visit order; use an index-based queue for levelOrder rather than repeatedly shifting an array.","Explain a reasoned answer to: Which traversal visits a Binary Search Tree's nodes in sorted order?","Explain a reasoned answer to: Level-order traversal uses a queue, not recursion."]
prerequisites: ["3/3-4/01"]
thresholdConcept: false
---

## Why this matters

Trees are only useful if you can visit their nodes — to search, process, or collect values. But unlike an array where you always go left to right, trees can be traversed in multiple valid orders, each with its own use case. Knowing which traversal to reach for, and why, is a fundamental skill you'll use in search algorithms, compilers, and interview problems.

## Start with something familiar

Touring a building can record each room on entry, between its wings, or after leaving both wings.

## The mental model

> Traversal order determines when a node is processed relative to its children. The same tree can yield different useful sequences.

## The four traversal strategies

Every traversal visits all n nodes exactly once, so all are O(n) in time. The difference is the **order** in which nodes are visited.

### Depth-first traversals (use recursion)

Three traversals explore as deep as possible before backtracking. They differ only in when the current node is processed relative to its children.

**Inorder: left → current → right**

```javascript
function inorder(node) {
  if (node === null) return [];
  return [...inorder(node.left), node.val, ...inorder(node.right)];
}
```

Why it matters: on a Binary Search Tree, inorder traversal visits nodes in **sorted ascending order**. It's the go-to for "give me all values sorted."

**Preorder: current → left → right**

```javascript
function preorder(node) {
  if (node === null) return [];
  return [node.val, ...preorder(node.left), ...preorder(node.right)];
}
```

Why it matters: the root is always first. This makes preorder ideal for **cloning or serializing** a tree — you can reconstruct the original tree by replaying insertions in preorder sequence.

**Postorder: left → right → current**

```javascript
function postorder(node) {
  if (node === null) return [];
  return [...postorder(node.left), ...postorder(node.right), node.val];
}
```

Why it matters: children are always processed before their parent. This is what you want when a parent's result depends on its children — computing subtree sizes, freeing memory (delete children before the parent), or evaluating expression trees.

### Level-order traversal (use a queue)

**Level-order (BFS): level by level, left to right**

Instead of recursion, level-order uses a **queue** (first in, first out):

```javascript
function levelOrder(root) {
  if (root === null) return [];
  const result = [];
  const queue = [root];

  while (queue.length > 0) {
    const node = queue.shift(); // dequeue from front
    result.push(node.val);
    if (node.left !== null) queue.push(node.left);
    if (node.right !== null) queue.push(node.right);
  }

  return result;
}
```

Why it matters: level-order finds the **shortest path** from the root to any target node (in terms of number of edges). It's also the natural way to print a tree level by level or to find nodes at a specific depth.

## Choosing the right traversal

| Goal                                      | Use                       |
| ----------------------------------------- | ------------------------- |
| Get BST values in sorted order            | Inorder                   |
| Copy or serialize a tree                  | Preorder                  |
| Compute subtree results before the parent | Postorder                 |
| Find nearest node to root, level-by-level | Level-order               |
| Determine tree height                     | Postorder (or either DFS) |

## Memory usage

All three depth-first traversals use O(h) memory, where h is the tree height — that's the size of the call stack. For a balanced tree, h = O(log n). For a degenerate tree (linked list shape), h = O(n).

Level-order uses O(w) memory, where w is the maximum width of the tree. For a perfect binary tree, the last level has n/2 nodes, so worst-case memory is O(n).

## The iterative inorder (bonus)

Recursive depth-first traversals can always be rewritten iteratively using an explicit stack. The iterative inorder is a common interview question because it's trickier than it looks:

```javascript
function inorderIterative(root) {
  const result = [];
  const stack = [];
  let curr = root;

  while (curr !== null || stack.length > 0) {
    // Go as far left as possible
    while (curr !== null) {
      stack.push(curr);
      curr = curr.left;
    }
    // Process the node, then move right
    curr = stack.pop();
    result.push(curr.val);
    curr = curr.right;
  }

  return result;
}
```

The key insight: push nodes as you go left, then pop to process and pivot right. The stack simulates the call stack of the recursive version.

## Guided recall

<FillBlank
  question="In preorder traversal, the ___ node is visited first, before its children."
  answer="root"
/>

## Check your understanding

<SandboxExercise
 language="javascript"
 instructions={"Implement inorder, preorder, postorder, and levelOrder for the supplied binary tree. Return arrays of values, and return an empty array for null. Preserve each traversal's stated visit order; use an index-based queue for levelOrder rather than repeatedly shifting an array."}
 initialCode={"class TreeNode {\nconstructor(val) {\n  this.val = val;\n  this.left = null;\n  this.right = null;\n}\n}\n\n// Build the tree:\n// 4\n// / \\\n// 2 6\n// / \\ / \\\n// 1 3 5 7\n\nconst root = new TreeNode(4);\nroot.left = new TreeNode(2);\nroot.right = new TreeNode(6);\nroot.left.left = new TreeNode(1);\nroot.left.right = new TreeNode(3);\nroot.right.left = new TreeNode(5);\nroot.right.right = new TreeNode(7);\n\nfunction inorder(node) {\nif (node === null) return [];\n// TODO: return left + current + right\n}\n\nfunction preorder(node) {\nif (node === null) return [];\n// TODO: return current + left + right\n}\n\nfunction postorder(node) {\nif (node === null) return [];\n// TODO: return left + right + current\n}\n\nfunction levelOrder(root) {\nif (root === null) return [];\nconst result = [];\nconst queue = [root];\n// TODO: process queue — dequeue, push val, enqueue children\nreturn result;\n}\n\nconsole.log(\"Inorder: \", inorder(root)); // [1,2,3,4,5,6,7]\nconsole.log(\"Preorder: \", preorder(root)); // [4,2,1,3,6,5,7]\nconsole.log(\"Postorder: \", postorder(root)); // [1,3,2,5,7,6,4]\nconsole.log(\"LevelOrder:\", levelOrder(root)); // [4,2,6,1,3,5,7]\n"}
 solution={"class TreeNode {\nconstructor(val) {\n  this.val = val;\n  this.left = null;\n  this.right = null;\n}\n}\n\n// Build the tree:\n// 4\n// / \\\n// 2 6\n// / \\ / \\\n// 1 3 5 7\n\nconst root = new TreeNode(4);\nroot.left = new TreeNode(2);\nroot.right = new TreeNode(6);\nroot.left.left = new TreeNode(1);\nroot.left.right = new TreeNode(3);\nroot.right.left = new TreeNode(5);\nroot.right.right = new TreeNode(7);\n\nfunction inorder(node) {\nreturn node === null ? [] : [...inorder(node.left), node.val, ...inorder(node.right)];\n}\n\nfunction preorder(node) {\nreturn node === null ? [] : [node.val, ...preorder(node.left), ...preorder(node.right)];\n}\n\nfunction postorder(node) {\nreturn node === null ? [] : [...postorder(node.left), ...postorder(node.right), node.val];\n}\n\nfunction levelOrder(root) {\nif (root === null) return []; const result = [], queue = [root]; for (let index = 0; index < queue.length; index++) { const node = queue[index]; result.push(node.val); if (node.left !== null) queue.push(node.left); if (node.right !== null) queue.push(node.right); } return result;\n}\n\nconsole.log(\"Inorder: \", inorder(root)); // [1,2,3,4,5,6,7]\nconsole.log(\"Preorder: \", preorder(root)); // [4,2,1,3,6,5,7]\nconsole.log(\"Postorder: \", postorder(root)); // [1,3,2,5,7,6,4]\nconsole.log(\"LevelOrder:\", levelOrder(root)); // [4,2,6,1,3,5,7]\n"}
 testCases={["The implementation preserves the stated invariant and produces the supplied example results"]}
/>

<Quiz
  question="Which traversal visits a Binary Search Tree's nodes in sorted order?"
  options={["Preorder", "Inorder", "Postorder", "Level-order"]}
  answer={1}
  explanation="Inorder traversal (left → current → right) on a BST visits nodes in ascending sorted order, because all left subtree values are smaller and all right subtree values are larger than the current node."
/>

<Quiz
  question="Level-order traversal uses a queue, not recursion."
  options={["True", "False"]}
  answer={0}
  explanation="True. Level-order (BFS) uses a queue to process nodes level by level. Depth-first traversals (inorder, preorder, postorder) use recursion (which implicitly uses the call stack) or an explicit stack."
/>

<Quiz
  question="Postorder traversal is best suited for which task?"
  options={[
    "Serializing a tree for storage",
    "Getting BST values in sorted order",
    "Computing a result that depends on subtrees",
    "Finding the nearest node to the root",
  ]}
  answer={2}
  explanation="Postorder processes children before parents, making it ideal when a parent's computation depends on its children's results — like computing subtree sizes, evaluating expression trees, or freeing memory bottom-up."
/>



## What you learned

- **Inorder** (left → node → right): produces sorted output on BSTs
- **Preorder** (node → left → right): root first — ideal for cloning or serializing
- **Postorder** (left → right → node): children before parent — ideal for bottom-up computation
- **Level-order** (BFS): uses a queue, visits nodes level by level
- All traversals are O(n) time; DFS uses O(h) space, BFS uses O(w) space
