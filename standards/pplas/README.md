# PPLAS-1.0 — Privacy-Preserving Learning Analytics Standard

Event catalog lives at `event-catalog.yaml` (created in Phase 3-A).

This directory is intentionally near-empty until the analytics architecture phase lands. The dir exists now so CODEOWNERS / standards-gate paths resolve correctly. **No analytics event may be added to the codebase that is not in this catalog** — the CI gate (Phase 4) enforces this.
