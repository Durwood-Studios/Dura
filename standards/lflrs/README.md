# LFLRS-1.0 — Local-First Learning Record Standard

Schema lives at `schema.json` (created in Phase 2-A).

This directory is intentionally near-empty until the CRDT-annotated learner record schema lands. The dir exists now so CODEOWNERS / standards-gate paths resolve correctly.
